# OmniSpace AI v2.1 后端依赖（规格 §2 技术栈锁定）
fastapi==0.115.0
uvicorn==0.32.0
pydantic==2.10.0
sqlalchemy==2.0.36
psutil==6.1.0
pynvml==12.0.0
aiofiles==24.1.0
PyYAML==6.0.2
lz4==4.3.3
# 以下为可选依赖（实际环境可能未安装，代码需做降级处理）
# celery==5.4.0   # 未启用：任务队列为进程内 PriorityQueue 模拟（见 lora_training_service）
# redis==5.2.0    # 未启用：缓存为进程内 data/cache.py 实现
# （sqlcipher3 已移除：数据库明文 SQLite，本地单机无需全库加密）
# chromadb==0.5.0
# pytorch==2.8.0
# diffusers==0.31.0
# transformers==4.46.0
# llama-cpp-python==0.3.0
