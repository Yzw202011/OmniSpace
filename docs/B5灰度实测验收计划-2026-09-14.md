# B5 unified 灰度实测验收计划（2026-09-14）

> 状态：**B 段实弹验收已执行完毕（当日 16:01~16:15），全部通过，附意外收获一枚**。
> 灰度已翻转（拍板=A，f2af511）：`task_queue.impl: unified` 默认在役。
> 回退锚：任意时刻 config 拨回 `legacy` 重启即原行为（镜像测试保证双实现语义一致）。

## 〇、B 段实弹验收结果（2026-09-14 执行存档）

| # | 场景 | 结果 | 证据 |
|---|---|---|---|
| B1 | 冷启动 E2E（unified 默认） | ✅ | health healthy/db ok；无 unified 异常日志。（注：首次点火遇并发 legacy 实例〔14:07 启动〕，按 §16.6 查无活动任务后 /api/quit 正规停掉重点火） |
| B2 | 生图全链 | ✅（修复后） | **队列语义首轮即全过**（功能锁→gpu_budget→开跑→收尾→error 诚实透传不悬挂）；出图被**既有 P1 缺陷**挡住：draw 链 DEFAULT_SAMPLER="euler_a"（legacy diffusers 常量）传入 comfy 工作流，ComfyUI 0.34 无此名（44 项列表）→ 校验失败。修复=comfy 侧别名映射 euler_a→euler_ancestral（comfy_paint_engine 两处组装点），复验 **done，39.3s，512×512 PNG 398KB 真出图** |
| B3 | 排队取消三态 | ✅ | 双任务入队→取消排队者返回 `was_pending:True`→终态 cancelled 且从未 running；在跑任务状态机收敛（error 透传） |
| B4 | 队列快照契约 | ✅ | pending=1/running=1 可见性正确；契约形状 {pending,running,pending_count,running_count}（首测探针读错键为虚惊） |
| B5 | 事件日志健康 | ✅ | call_image_ok/fail 记录形态与 legacy 基线一致 |
| B6 | 资源复原 | ✅ | /api/quit 后三端口全释放、显存 846MiB 基线、无孤儿 |

**意外收获（B5 范围外既有缺陷，已修）**：绘画页默认参数生图全断（sampler 名跨栈失配）——
若非 B2 实弹全链验证不会暴露。修复独立可回退。

## 一、背景与范围

统一任务子系统（B5）已施工完毕：TaskQueueCore 核心 + image/video 双模接入 + 10 条镜像测试。
生产默认已切 unified。本计划覆盖：**已完成的自动化验证**、**本轮实弹验收（当天执行）**、
**观察期核对（~09-17，期满终验）**。不在范围：novel/训练队列（登记例外不迁，见 v4 §二十四）。

## 二、A 段——自动化验证（已完成，证据存档）

| 项 | 结果 |
|---|---|
| 双实现语义对齐 | image 镜像 5 条 + video 镜像 5 条（3 连跑稳定），任何一红=unified 不可上线 |
| 翻转大考（unified 默认下全部 legacy 金标准零改稿） | 首轮抓 3 接缝缺口（video 状态机/锁旗标割裂/云道命名漂移）逐一修复后全绿 |
| 全量回归 | 774 绿（翻转后多次连跑）；ruff 全仓过；mypy 零新增（基线 215） |
| 优先级单源 | lora/style PRIORITY_LEVELS 收敛 priority.py（is 同一 dict 实证） |
| 巡检工厂实弹 | periodic 立即首跑+间隔轮转+stop 停止全验证 |

## 三、B 段——本轮实弹验收（当天，GPU 窗口）

前置纪律：GPU 活动门（核查无并发会话/无 >1GB comfy 进程/栈停）；全程 boot 正规链点火、/api/quit 正规退出。

| # | 场景 | 操作 | 通过标准 |
|---|---|---|---|
| B1 | 冷启动 E2E（unified 默认） | boot 点火 | /health 200；日志无 unified 相关异常；模型接线与术前基线一致 |
| B2 | **生图全链（unified 真队列）** | POST /draw/generate（小尺寸低步）→ 轮询 result | 任务经 unified 队列：入队位次 → 开跑 → 出图；events 日志有对应记录；HTTP 不悬挂（B0 超时修复路径） |
| B3 | **排队取消三态** | 连发两个任务，取消排队中者 | cancel 返回 queued；被取消任务不执行；另一任务正常完成 |
| B4 | 队列快照契约 | 生图过程中 GET /draw/queue | snapshot 形状与前端契约一致（queued 列表/current/lock_held） |
| B5 | 事件日志健康 | 收尾后核对当日 events | 任务成功/失败记录形态与 legacy 基线一致；无新增异常类 |
| B6 | 资源复原 | /api/quit 正规退出 | 5800/5850/8189 无监听；无孤儿进程；显存回基线 |
| B7 | boot E2E 第二次（观察期锚点） | 复启即停（可选） | 与 B1 一致 |

## 四、C 段——观察期核对（~09-17，期满终验）

1. **高优：捕获「全量偶发 1 挂」**（10 轮中 2 次、复跑即绿、名字未捕获）——每日全量跑带 `-rf`，捕获后定性（unified 缺陷则修复或拨回 legacy）。
2. 使用中任务成功率/排队位次显示/取消响应与 legacy 基线对比不劣化（用户日常使用即实测）。
3. 期满无异常：**删 legacy 内联实现**（image/video 约 -400 行）+ git 记录；B5 终验收归档。

## 五、通过标准与回退

- **通过**：B 段全过 + C 段 3 天观察无缺陷 + 偶发挂捕获定性（或确认为低危测试基建问题）。
- **不通过**：任一实弹场景失败且不可快速修复 → config 拨回 legacy（秒级），缺陷转 B5 缺陷单，修复后重新灰度。
- 回退代价：一次 config 修改 + 重启；无数据迁移。

## 六、下一轮约定（深度实测）

B5 验收完结后，另行开展一轮**全功能深度实测**（真人操作流全页面 + 全生成链实弹 +
多日稳定性浸泡），计划单独立项成文。本计划不覆盖。
