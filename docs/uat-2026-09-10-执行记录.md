# 全应用真人操作级测试 · 执行记录（2026-09-10）

> 依据：用户《AI 对话（所有模型都要进行测试）》清单（62 项 / 5 模块）。
> 纪律：前端模仿真人操作（IAB 浏览器真实点击/输入），后端实时监控定位。
> 标记：✅ 通过 · 🔶 部分验证/机制级 · ⚠️ 未验证 · ❌ 失败 · 🚫 产品缺口。
> 只能输入中文：所有测试输入均为中文。

## 〇、测试环境与预检发现

- 启动方式：全新点火 boot.py（今日全部后端修复首次生效：消毒校验/卸载等推理/账本修正等）。
- 本地模型清单（dialog/status 实测）：qwen35-9b-w4a16 / qwen3-vl-4b / qwen3-vl-8b-awq。
  （GGUF 9B 已于今日按用户拍板删除；云端 DeepSeek/万相 Key 在库。）
- **预检事故 1（已排查）**：用户说已关软件但 5800 仍活 → stop.py 正规链优雅退出。
- **预检事故 2（已排查，产品缺口）**：stop 后孤儿 `llama-server.exe`（上一实例 GGUF 9B
  子进程）持 8.6GB 显存不释放——**stop.py 清理面不含 llama-server**（09-06 孤儿 LLM 同款，
  第二次发生）。已按先例 taskkill 整树清障，显存回收至 2.0GB（空闲 ~14.2GB）。
  → 建议列入待拍板：stop.py 清理面 + llama 孤儿收账时机。
- **幽灵驱动者（已按用户令排查）**：15:07 vLLM 8B 加载 / 15:23 paint 加载 / 15:24 切
  video_gen / 15:32 小说 AI设计角色（15:44 因模型未就绪异常收尾）/ 15:55 切 training——
  动作顺序与测试清单模块顺序吻合；学习模块自走已排除；用户确认本人未使用。
  用户关闭软件后未再复现。**疑点未定案**：若复发即为高优事故。

## 一、AI对话

### 1-1 新建对话 ✅
16:30 点击「新建对话」→ 列表新增会话并自动选中，主区切会话欢迎语。

### 1-4 模型选择与加载 / 1-10 自愈机制 —— ❌ 发现三连 bug（8B 场景）
- **F1**：8B vLLM 第一次启动 63s 后 proc_exit code=1（原因待深挖，疑与今日
  体积清理误伤 .cache/triton 有关——第二次尝试仅警告并成功编译）。
- **F2**：降级链方向错误：8B 失败后下一个候选是 **9B w4a16（更大）**，
  被准入闸以「13.4GB<14.9GB」拒绝——链按登记表顺序而非体积降序。
- **F3（最重）**：vLLM 自动重试 91s 后 ready（16:32:06），**1 秒后**被降级链
  kill process tree——降级链与自动重试/V9-γ 收养互相拆台，引擎终态 error。
- 用户侧呈现：错误文案诚实带出路（✅），但内容是 **9B 的下限**（用户选的
  8B）——张冠李戴；「重新生成」按钮与「上次加载未成功·发送时自动重试」
  状态栏诚实可见（✅）；加载期导航自动禁用重量级模块（✅ 正确互斥）。
- 【复测中】点击「重新生成」验证自愈重试。
- 【复测结果 ✅】重新生成→冷启动提示合规（"首次加载约 2-3 分钟，请勿切换页面"），
  91s ready（V9-γ 收养正常），8B 正常回复，深度思考开启态输出完整推理路径
  （【问题分析】→【最终回答】）。
### 1-7 深度思考 ✅
开=完整推理路径+最终回答；关=直接一句话+「答复模型：Qwen3-VL 8B · 旗舰」
归属标签。两态差异明确，与开关描述文案一致。
### 1-5/1-6 温度/上下文 🔶（机制级）
控件存在（温度 0-2 滑杆默认 1.0；上下文 2048/4096/8192），面板注明
「随每次发送生效」。行为级差异未测（温度对采样的影响无法断言归因）。
### 1-8 图片理解 ✅
API 实弹（IAB 无法操作 OS 文件选择框，如实声明）：PIL 画红圆 → 4B 视觉模型
9s 正确回答「红色圆形」。上传图片按钮随模型变化见 1-12。
### 1-9 文档理解 ✅
中文 txt → /dialog/parse-document 正确解析（45 字/txt）→ 资料问答「罗盘材质」
→ 正确回答「黄铜」。txt/md/docx/pdf 管线提示在位；.doc 诚实拒绝设计在案。
### 1-2 批量删除 ✅（三层验证）
批量管理→全选→删除：确认弹窗含不可恢复警告 ✅；前端列表清空 ✅；
后端 sessions=0 ✅；SQLite 直查 0 会话 0 消息零残留 ✅。
### 1-3 直接输入自动建对话 ✅
空态输入「这是一条直接输入的自动建对话测试」→ 发送即建会话（后端 0→1），
空态引导文案「直接在下方输入内容即可自动创建对话」在位。
### 1-11 云端 API ✅
DeepSeek v4-flash 真实往返（归属标签 cloud::prov_bc20002d5c::deepseek-v4-flash），
回答正确（三幕式结构）。
### 1-12 上传能力随模型变化 ✅（降级态一处瑕疵 🔶）
8B/4B（VL）显示图片+文档双按钮；纯文本态只留文档按钮。瑕疵：9B 降级到
4B（VL）后前端按「所选模型（9B 纯文本）」而非「实际答复模型（4B 视觉）」
计算能力，误隐藏图片上传。
### 1-13 上下文/深度思考随模型变化 ⚠️ 未随模型变化
上下文选项固定 2048/4096/8192 三档、深度思考开关常驻，本地与云端一致
——未观察到模型级差异（可能设计如此，如实记录）。
### 1-14 文字描述 ❌ 发现真 bug：会话日期全部显示「1/22」
根因定位（文件:行号）：SessionList.tsx:338 formatDate(s.updatedAt) ←
后端秒级时间戳被 new Date() 当毫秒 → 1970-01-22（UTC+8）。后端数据正确
（API 实测 created_at=1789030291=今天）。修法=MangaLibrary formatTs 同款
单位防护。1/22 触发的会话卡日期标签全部中招。
### 1-15 使用引导 🚫 产品缺口
对话页无任何新手引导/悬浮帮助按钮（全文检索 引导|新手|guide 零命中）。
清单要求「首次强制引导+可隐藏悬浮按钮（重启恢复）」未实现。
### 1-16 页面健康 ✅
刷新后布局完整（截图取证），状态栏 CPU 4%/GPU 10%/显存 12.1/15.9GB/
知识 1,312 条/已联网 全部正常；加载期导航互斥禁用、批量确认弹窗、
错误文案带出路等 UX 细节到位。

## 二、AI漫画（进行中）

### 2-1 新建漫画 ✅
弹窗（名称+30 画风卡）→「UAT漫画测试0910」+日系动漫 → 创建并进入编辑器 ✅，
项目级画风选中生效。
### 2-3 画风选择 ✅（选择面+生效面）
弹窗 30 画风卡全量在位；创建后项目级画风下拉同步选中；切画风后重新生成分格
即生效的提示在位（全画风出图逐测不现实，机制级 ✅ + 单画风实弹见 2-6）。
### 2-5/2-6 AI 建角色 ✅
「林小满」+中文形象描述 → 四视图生成成功（~3.5 分钟，klein-4b 底座自动切换
1.3s，gpu-budget 准入 granted，功能锁重入计数正常），角色卡（图+名+描述）
入库，弹窗自动关，显存用后自动释放（2.4GB）。
### 2-11 上传入口 ✅（存在性）
角色栏「上传角色图/上传本地角色图」+「资产库」入口在位（OS 文件对话框
自动化不可测，真实上传走 API 验证在 1-8/1-9 已覆盖同类管线）。
### 2-12 模型加载进度 ✅
生成中按钮态「生成四视图中…」+取消禁用；首次发送冷启动提示合规（模块一）。
### 观察项（非清单）：预加载器候选表仍引用已删模型（klein-9b/8B）反复告警，
噪音点建议清理。
### 2-13/2-14 角色绑定与一致性 ✅（机制+实弹）
分格「引用资产」下拉含林小满；绑定后出现「林小满的台词」独立输入位
（多角色各说各话结构在位）；生成时引擎自动选路 comfy+PuLID（chars=1/
face_ref=True，身份硬锁生效），klein-4b 画风仲裁正常。单格 141s 出图。
### 2-17 添加分格/生成 ✅（含一处自动化工件说明）
添加分格即建编辑卡（描述/台词/引用资产/生成画面/删除本格）；单格生成
全链通。**重要过程记录**：合成事件（JS fill）填写的描述不进保存链→
生成报「分镜行无画面描述」；真键盘 type+真点击失焦后保存正常、生成
成功——真人路径无此问题（自动化工件，不计产品缺陷；已用 DB+API 双向
定位：保存端点本身完好，绑定 asset_ids 保存成功而文字未保存=触发链差异）。
### 2-15/2-16/2-18 预览与气泡 🔶
台词气泡实时渲染在分格卡上，带「拖动移动位置；右下角把手拉伸宽度」
（2-18 交互元素在位；实际拖拽为鼠标手势，自动化受限未实测，历史 UAT
09-08 已验证拖拽/拉伸）。预览按钮点击后未观察到明显视图切换（🔶 待
单会话复验）；导出成册按钮随分格完成解禁（2-12 关联证据）。
### 2-19 云端 API 🚫/⚠️（按用户挂起裁定）
漫画工作台内无云端工位绑定面板（复苏清单 C1 项，用户 09-08 拍板先本地）；
底层云端图像路由在库（image_queue cloud=True 通道存在）。如实记为挂起。
### 2-20 使用引导 🚫 产品缺口
漫画页同样无新手引导/悬浮帮助按钮（与 1-15 同族缺口）。
### 2-21 页面健康 ✅
状态栏全程正常，生成期/空闲期无异常报错弹窗。
### 2-2 删除 / 2-4 AI写分格 / 2-7~2-10 场景道具 / 2-3 全画风出图 ⏳
（见下方补测节）

## 三、写作台

### 3-1/3-2 新建作品 ✅（配置项与清单描述有出入）
创建《UAT写作测试》（名/题材/灵感三字段）。清单所述「大纲数、章节数量、
章节字数」输入项**不存在**——由 AI 生成时自动决定（如实记录为设计现状）。
### 3-3 AI 生成大纲 ✅
「卷→章细纲」树全自动生成（明日送达 卷 + 9 章），每节点可改/删/收起，
正文章节自动建好，内容与灵感高度贴合，8B 模型槽位记忆正常（写作台槽自动
选 8B，与对话页 4B 互不干扰）。生成约 3.5 分钟。
### 3-6 AI 设计角色 🔶（UI 在位，实弹转由 3-4 优先）
角色页「AI 设计 + 补充要求 + 手工添加表单」全在位；15:32 幽灵的同类任务
失败根因=当时显存不足装载失败（资源问题非功能缺陷），本轮 GPU 让给 3-4。

## 三、写作台（续）
### 3-4 生成未完成章节 ✅
8 章批量生成全部完成（每章 20s 级，合计 27,160 字，8B vLLM）。
瑕疵 🔶：任务完成后书架卡片仍显示「正在执行」，刷新页面才更新为
「8/8 章完成 · 27160 字」——任务完成未主动刷新书架状态。
### 3-11 整本导出 ✅ 机制 / ❌ 内容尾部
TXT 导出成功（27,337 字符，8 章齐全，标题/灵感头部正确，开头质量高）。
**发现生成质量缺陷**：第 8 章（尾声）结尾出现 LLM 退化文本——金融词
无限 salad（「偿还还款分期付款保证金信用额度…」），疑似采样循环/截断
异常。单章字数（3362）正常，属模型尾部退化，建议列入修复观察。
### 3-5/3-7/3-8 ✅（入口与表单在位）
新增章按钮、手工添加角色表单（角色名/定位/人设）、伏笔/世界观面板
均在位（实弹生成以 3-3/3-4 覆盖主链，其余入口级验证）。
### 3-9 云端 ⚠️（写作台云槽在模型下拉可见：DeepSeek 系可用，同 1-11）
### 3-10 使用引导 🚫（与 1-15/2-20 同族缺口，全应用无引导系统）

## 四、知识学习
### 4-1 自动学习 ✅ 「让AI自己决定学什么」按钮在位（未实弹执行，GPU 让位章节）
### 4-2 查看日志 ✅ 真实日志在档（09-09 浏览轨迹+验证码/登录墙规避记录）
### 4-3 查看浏览器 ✅ 内置浏览器面板：AI 控制中/用户接管切换+地址栏在位
### 4-4 添加新主题 ✅ 「国产动画画风演变」创建成功入列
### 4-5 行为学习 ✅ 已记录 2,140 操作，偏好模型学习中，下次微调定时在案
### 4-6 知识库 🔶 1,312 条/15.6MB、列表/图谱双视图、搜索/删除/图谱上下文在位；
**批量删除 ❌ 无**；**图片知识 ❌ 不支持**（清单要求的两项缺口）
### 4-7 导入文档 🔶 入口在位（解析管线同 1-9 已实弹）；重量级导入未重复跑
### 4-8 学习设置 🔶 区块在位，逐项开关未逐一拨动
### 4-9 训练任务 ⏳ GPU 让位章节生成，未实弹（微调链路 09-08 曾全链验证）
### 4-10 页面健康 ✅

## 五、模型管理
### 5-1 模块配置生效 ✅ 行为级证据：写作台槽自动选 8B、对话页选 4B、
漫画生成自动切 klein-4b——各模块槽位独立记忆并真实路由。
### 5-2 重启校验 ✅ 今日全新启动即全量登记表对账（上午幽灵条目正是被
该闸拦下）；登记表测试 6/6 常绿。
### 5-3 页面健康 ✅ 功能模块配置区/模型清单/显存面板齐全。
### 2-2 删除漫画 ✅（三重验证）
API 建一次性项目 → UI 卡片删除（确认弹窗含「分格、关键帧与项目内角色资产
将被清理（资产转入全局库保留）。此操作不可撤销」）→ UI 消失 ✅ + 后端列表
无此项目 ✅ + 前后端口径一致 ✅。

---

# 总结账（2026-09-10）

## 测试结果总账
| 模块 | 项数 | ✅ | ❌/🚫 | 🔶/⚠️ |
|---|---|---|---|---|
| 一 AI对话 | 16 | 11 | 1 bug已修+1🚫 | 2🔶+1⚠️+1🔶 |
| 二 AI漫画 | 21 | 13 | 2🚫 | 6🔶（含删除✅计13✅） |
| 三 写作台 | 12 | 7 | 1❌（章节尾部退化）+1🚫 | 3🔶 |
| 四 知识学习 | 10 | 7 | 2❌（批量删除/图片知识） | 1⏳+3🔶（重叠计入） |
| 五 模型管理 | 3 | 3 | 0 | 0 |

## 新发现缺陷与缺口（按严重度）
1. 【对话·高】8B vLLM 首启 proc_exit code=1 + 降级链反向选 9B + **链杀死
   ready 的 8B（竞态）**——三连相关缺陷，自愈链终态 error（本轮复测第二次
   启动成功，问题集中在「首启失败后的链 coordination」）。
2. 【对话·中】会话日期全部 1/22（秒当毫秒）——**已修复入库**。
3. 【写作·中】长文尾部 LLM 退化（第 8 章结尾金融词 salad）。
4. 【对话·低】降级态上传能力按「所选模型」而非「实际答复模型」计算。
5. 【写作·低】章节完成后书架不自动刷新。
6. 【运维·中】stop.py 不清 llama-server 孤儿（今日两事故同源：占 8.6GB 显存）。
7. 【运维·低】预加载候选表引用已删模型（klein-9b 等）反复告警。
8. 【产品缺口】①全应用无新手引导系统（对话/漫画/写作三项清单要求）；
   ②知识库无批量删除；③知识库不支持图片知识；④漫画云端工位绑定面板
   （对应云端挂起裁定，非缺陷）。

## 未测/部分测项的诚实声明
- 全部 30 画风逐画风出图、场景/道具全型生成、训练任务实弹、自动学习实弹、
  气泡鼠标拖拽手势：未逐一执行（GPU 时长与自动化手势限制），以机制验证+
  同型实弹覆盖，均已在正文标注 🔶/⏳。
- 图片/文档上传的 OS 文件选择框无法自动化：走后端 API 实弹（真实文件+
  真实模型理解），GUI 按钮存在性已验。
- 15:07-15:55 幽灵驱动者事件：用户关闭软件后未复发，根因未定案（已排除
  学习模块自走；若复发属最高优排查项）。

---

# 补充轮：缺陷修复 + 漫画多分格多角色一致性专项（2026-09-10 晚）

## 缺陷修复结果
1. ✅ **缺陷6 stop.py 孤儿清理**（已入库）：新增 _llama_leftovers()（匹配
   llama-poc / OmniSpace-LLM.exe），优雅退出与兜底双路径收账。实弹验证：
   造假孤儿+真实残留共 4 个进程全部终止（含 15020 假孤儿与 3 个真实残留）。
2. ✅ **缺陷7 预载引用已删模型**：module-config 经产品 API 修正——paint/
   manga-paint 默认 flux2-klein-9b(已删)→flux2-klein-4b；dialog/manga-dialog
   的 allowed 移除已删模型；**manga-video 槽按 H3 冻结令原样未动**。
3. ⏳ **缺陷1（对话自愈链竞态 F2/F3）**：并发会话仍整文件占用
   dialog_engine.py，修复设计已备：①降级候选按预估显存升序而非登记表序
   （F2）；②切换卸载前复核「当前 ready 后端是否已可用」防杀刚就绪引擎
   （F3）。文件让位即可落地（≤1 小时）。

## 漫画多分格多角色一致性专项结果

四格剧本：格1 林小满窗边（单人）/ 格2 林小满特写 / 格3 林小满+陈默双人
同框 / 格4 陈默窗边（单人）。两个 AI 角色（四视图锚点）+逐格绑定+生成。

| 格 | 路由 | 一致性判定 |
|---|---|---|
| 2 林小满特写 | comfy+PuLID（chars=1） | ✅ 黑发/红发卡/白衬衫蓝背带/黄铜罗盘/专注表情全命中 |
| 4 陈默单人 | comfy+PuLID（chars=1） | ✅ 黑框眼镜/深灰卫衣/逆光构图全命中，画风与格2统一 |
| 3 双人同框 | **diffusers（chars=2，无锁）** | ❌ 画风漂移（吉卜力淡彩≠日系线风）、场景偏题（教室人群≠走廊交谈）、角色服装发型与锚点不符、罗盘消失 |

### 根因（日志铁证 keyframe.py:1204）
`引擎自动选路: diffusers（P0-1 PuLID 条件 env=on comfy=True pulid=True
**chars=2** face_ref=True）`——PuLID 身份硬锁的启用条件把 chars 写死为 1，
多角色面板必然走无身份锁的 diffusers 路线 → 画风与角色双漂移。单角色
一致性机制本身优秀（锚点四视图+PuLID 硬锁实测命中）。

### 修复方向（待拍板，动核心一致性机器须出方案）
把 chars≤2（或 N）纳入 comfy+PuLID 多参考路由（ComfyUI 工作流多参考链
09-08 曾实弹验证过），或为多角色引入专用一致性工作流；改路由条件一行+
多参考工作流回归，属核心链路改造。

## 多角色一致性路由 Step A 落地（2026-09-10 晚，实弹验收通过）

- 改动：keyframe.py 路由条件 `==1`→`1<=chars<=2`（:1229）+ 参考装配抽
  `_comfy_ref_entries`（PuLID off/pos、单角色、多角色四分支等价，face
  排除规则统一）+ 选路日志诚实化（comfy+多参考 标签）。
- 哨兵：test_keyframe_multichar_refs.py 5 例全绿；既有 keyframe 测试
  19 例零回归；ruff/mypy ✅。
- 实弹：格3（双角色）重生成走 `comfy+多参考（chars=2）`，v2 216s 出图；
  四条验收全中（画风统一/林小满锚/陈默锚/构图符合）——对比 v1 的
  双漂移根治。回归：格2 单角色重生成路由不变（comfy+PuLID chars=1），
  112s 出图。
- 诚实微调说明：Step A 落地取「双锚参考软锁」变体（不挂单侧 PuLID，
  避开错锁风险且为 09-08 验证过的链）；主角色 PuLID 增强并入 Step B
  双锁 PoC 一起裁决。
- Step B（双 ApplyPuLIDFlux2 级联 PoC）：待跑（引擎参数草图在方案 §Step B）。

## Step B 双 PuLID 级联 PoC 结果（2026-09-10 晚，成立 ✅）

- 引擎改造：comfy_paint_engine 支持 pulid_image_b/pulid_strength_b
  （第二 ApplyPuLIDFlux2 attention 级联，参数驱动，默认零影响）。
- 实验配置：双角色立绘入 ReferenceLatent + 林小满 closeup PuLID@0.9 +
  陈默 closeup PuLID-B@0.8，1280x720 36步，223s 出图
  （data/keyframes/_poc_dualpulid.png）。
- 目验判定：双身份并存且无病理（无特征混合/无双脸/无畸变）；林小满
  （发卡/背带裙）与陈默（眼镜/卫衣）锚点命中；构图与画风合格。
  身份锐度较 Step A 软锁更优（B 角色眼镜/卫衣特征更咬合）。
- **转正前置（下一个小步，未做）**：产品路由接线需解决 face_refs 列表
  与 char_assets 顺序的映射校验（错锁防护的最后一环），建议单独小批
  次 + 双人格回归后再切默认。

## 双 PuLID 转正落地（2026-09-11 凌晨，实弹验收通过 ✅）

- 前置闭环：face_refs↔char_assets 映射校验通过（代码铁证：两者同源
  _fetch_bound_assets 同序过滤，_load_face_references docstring 明示
  P0-3「与绑定 character 资产序对齐（含缺失位 None）」——错锁防护的
  协议依据）。
- 接线：keyframe.py 新增 _dual_pulid_faces()（chars=2 且双 face 齐备
  →(faceA,faceB)，任一缺失/数量不符→None 回落软锁）；chars=2 双锁
  强度 A=_PULID_STRENGTH/B=0.8（PoC 验证值），reflatent 强制 both
  （PoC 同配置）；D-LoRA 在场双锁全关（身份归 LoRA 权重）；选路日志
  新增「comfy+双PuLID」标签。
- 哨兵 +3（双 face/缺 face/数量守卫）。
- **实弹**：格3 重生成走 `comfy+双PuLID（chars=2 face_ref=True）`
  （日志 1253 行），v3 238s 出图——四条验收全中，陈默眼镜/卫衣锐度
  较软锁 v2 更优，无身份混合病理。三连胜闭环：
  v1 无锁（漂移）→ v2 软锁（达标）→ v3 双硬锁（达标+锐度↑）。

## 缺陷④⑤ 修复（2026-09-11）

### ④ 降级态附件能力 ✅ 实弹
DialogPage.tsx：附件能力（vision）从「所选模型」改为跟随「最后一条带
engine 标注的答复模型」，无标注回退所选模型（存量兼容）。实弹：DeepSeek
文本答复的会话 → 图片上传正确隐藏（修复前误显示双按钮），提示「当前
模型为纯文本」。tsc 0 / vitest 71 / build ✅。

### ⑤ 书架任务收尾自动刷新 ✅ 机制级
useNovelStore.pollProgress：任务收尾拍（active→idle）回刷项目卡数据
（完成数/字数），书架不再停留「正在执行」。机制级验证（未重跑 8 章
生成做目击——GPU 时长考量；逻辑为收尾拍单行回刷，风险低）。

## 模型删除正规通道·根修（2026-09-11，缺陷⑨）

**定性修正**：正规删除端点早已存在（DELETE /models/{id} 除名、
/models/{id}/files 删盘，卸载→删盘→清库→缓存失效链完整），病根是
**models_manifest.json 成了只读孤岛**——两个删除端点都不同步它，而
提交闸对账的恰恰是 manifest。三种正确用法（删盘同步/纯除名/回补）
里的「删盘同步」缺失 = 幽灵条目结构性必现。
- model_registry 新增 remove_manifest_entry（命中移除落盘+失效缓存；
  损坏清单不覆写——对齐模块诚实边界），哨兵 4 例
- purge 端点接入同步 + required=true 随包模型拒绝删盘护栏
- 纯除名不碰 manifest（防孤儿目录告警），语义文档化
闸门：哨兵 10 例+registry 6 例全绿 / ruff / mypy / smoke 94 ✅。
**当日三犯（deepseek/GGUF/qwen-edit+wan）自此从源头封死；直接 raw rm
仍会绕过（纪律问题由提交闸兜底）。**

### 缺陷④⑤ 修复与模型删除根修（补录 2026-09-11）
- ④⑤ 已入库 ccfdaaf（详见上节）；模型删除通道根修入库（manifest 同步
  + required 护栏，本节上）——「一天三犯」病根自源头封死。

## 知识库批量删除补齐（2026-09-11，4-6 缺口①关闭）

- 定性修正：后端批量端点早已存在（DELETE /learn/knowledge/delete，
  ids 上限 500），缺口纯在**前端 UI 无批量入口**。
- 前端补齐：learningApi.batchDeleteKnowledge + store 动作 +
  KnowledgeBrowser 批量模式（勾选/全选本页/两段式确认/收尾退出）。
- 实弹：真实导入管线造 2 条测试知识（topic=UAT测试）→ UI 批量管理
  勾选 → 两段确认 → toast「已删除 2 条知识」+ 列表消失 + DB 零残留 ✅。
- 过程发现（如实记录，非本轮范围）：①/knowledge/list 的 query 参数
  疑似未生效（任意关键词返回同 10 条）；②导入管线在 LLM 未加载时
  提取 0 条静默返回（诚实降级但提示可改「模型未就绪」）。

## P1-3 腾挪外提落地（2026-09-11 凌晨，GPU 窗口实弹通过 ✅）

paint_engine.load_model 重构：依赖探测/选模型/显存腾挪全部移出引擎锁
（腾挪经 mgr 卸载对话引擎原持画锁跨引擎等锁=ABBA 画侧一边）；锁内二次
复核目标（并发改动幂等）+复位+真装载。错误路径状态写入改锁内小块。
**实弹**：对话 4B 在载（含 LoRA v4）时触发绘画加载——腾挪卸载对话、
klein-4b 装载、出图全链 0 死锁（日志 04:10:19→04:10:21 序列在案）。
ruff/mypy/paint 哨兵 5 passed ✅。P1-3 至此闭环（宣言+外提+实弹）。

## 第 8 章退化文本处置（2026-09-11）

- 检测器两次尝试均验证失败（诚实记录）：①尾部功能词密度——干净章节
  尾部同为 0%（动漫文尾部动作句多）；②尾部句末标点距离——干净章节
  同样 121 字无句读。正则级启发式无法区分语义退化，**需语义级方案
  （LLM 自检/质量模型）另立项**，不硬塞坏检测器。
- 数据修复：第 8 章经产品生成链重生成（8B），新尾部干净（文学化排比
  收尾+合格收束句），退化样本已被替换；字数 1302（前 3362）。采样
  退化属概率事件，既有防复读双发链继续兜底。

## 对话自愈链收尾（2026-09-11，并发会话交卷后）

- **交卷入库 48c016a**：并发会话 17 文件（云端路由/vram_policy/dialog_engine
  重写/一键 LoRA 脚本等），闸门三重验证通过后整体入库（本会话未逐行审计）。
- **F2 ✅**：_vram_fallback_model 拟合判据改 mgr.estimate_vram_gb 权威口径
  （候选表静态值偏低曾致 8B 失败反向回落 9B）——源码哨兵锁定。
- **F3 ✅**：降级决策前竞态守卫——ready 即可用严禁杀刚就绪引擎（源码哨兵）。
- **P1-23 ✅**：看门狗卸载后 release_stale 同步台账（源码哨兵）。
- **P1-18 ⚠️ 实现了但实弹未达预期**：SSE 断连监视器（1s 轮询
  request.is_disconnected→哨兵事件→既有 stop_check 停推理）+断连跳过
  被动补全+watcher 收尾。裸 socket 实弹：客户端硬断后 watcher 未触发，
  生成跑满 40s（GPU 空烧复现）；部分落库/锁释放经异常路径正常。
  待查=starlette is_disconnected 的 CancelScope 消费语义/uvicorn 断连
  消息投递时机——下一轮加服务端调试日志定位。

## 悬案销案 + P1-18 平台限制结论（2026-09-11，用户裁定）

- **幽灵驱动者销案**（用户裁定：人工排查完毕）：15:07-15:55 的驱动事件
  已排查（学习模块自走排除/未复发），按用户令关闭，不再追踪。
- **P1-18 平台限制结论**：uvicorn h11 下客户端 FIN 半关闭不向 ASGI
  投递 disconnect，is_disconnected 轮询探测不到；前端 pagehide +
  keepalive /chat/stop 兜底实测也未送达（IAB 页签硬关闭跳过卸载事件，
  生成仍跑满 65.7s/837 片段）。可用的停止通道 = 手动停止按钮（已验证）
  + 生成天然有界（1-2 分钟）。此为平台层限制，记录在案不再追。

## 悬案与清单收口（同轮）

- **未完成清单**：模型删除正规入口已销账（根修 a5883f5）；对话自愈链
  F2/F3/P1-23 已修入库（686535a/85369e5）；知识库批量删除已补齐
  （5dddcf7）。剩余=对话引擎三件中的 P1-18 平台限制（已定性）。
- **数据残留**：测试会话/项目保留作证据（漫画三版对比/小说 8 章），
  5 条「未命名项目」仍待用户判断。
