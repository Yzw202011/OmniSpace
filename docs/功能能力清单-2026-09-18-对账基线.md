# OmniSpace 功能能力清单（2026-09-18 · 对账基线）

> **性质**：监督会话产出的只读盘点基线，供后续对账（功能宣称 ↔ 代码事实）。
> **方法与诚实声明**：本清单由**代码枚举**生成——前端路由表/组件目录、后端装饰器计数（`@router.get/post/put/delete/websocket`）、引擎/服务文件清单、`src/config.yaml` 门控实值、磁盘模型资产。**未做逐功能运行时实测**；验证状态分级：
> ✅=代码结构确凿存在（附证据位）· 🔶=存在但处于门控/休眠/待验收态 · ⚠️=仅文档宣称或口径存疑。
> 快照时点：2026-09-18 21:00 前后（HEAD≈`b64d176`，版本口径 v2.5.0）。

---

## 一、前端层（React 19 + Zustand 5 + Vite 6）

**一级路由 11 条**（✅ `frontend/src/router.tsx` 实测；勘误：v1 首发误记 12，2026-09-18 深夜复核为 11——/style 现为重定向 /training，详见 CLAUDE.md §2.3）：
`/chat` 对话 · `/paint` AI漫画 · `/storyboard` 漫剧创作 · `/novel` 小说 · `/learning` 学习会话 · `/learn` 学习训练 · `/models` 模型管理 · `/style` 风格 · `/training` **训练中心（新增，CLAUDE.md 旧表未收录）** · `/settings` 设置 · `/logs` 日志 · `/help` 帮助。
组件域 13 个（comic/dialog/manga/novel/learning/learn/model/style/training/logs/help/layout/common，✅ `components/` 目录）+ 设置区六件（云API/数据管理/健康检查/插件/升级/联网搜索，✅ Settings 域文件）。组件总量 **81 个 .tsx**（✅ find 计数）。Store **12**（useApp/Dialog/Manga含子切片/Hardware/Learn/Learning/Model/Novel/Style/Task/Warmup，✅ `stores/`）。

## 二、后端 API 层（FastAPI，`src/api/`）

**端点总数 357（装饰器实测）+ 4 个 WebSocket**。分模块（端点数 ✅ 计数）：

| 模块 | 端点 | 模块 | 端点 |
|---|---|---|---|
| system 38 | novel 28 | models 28 | knowledge 27 |
| draw 24 | style 23 | dialog 22 | comic_asset 21 |
| learning 16 | storyboard 14 | comic 13 | logs 13 |
| video 12 | learn 12 | plugins 11 | cloud 10 |
| keyframe 7 | browser 7 | upgrade 6 | voice(manga) 6 |
| hardware 6 | character_lora 6 | license 2 | voice/vision_tools/training/comic_script/comic_export 各1 |

WS 4（✅）：`/ws` 消息中枢、`/dialog/stream/{id}`（废弃兼容）、`/hardware/realtime`、`/learn/session/progress`。

## 三、引擎与推理层（`src/engines/` + `src/services/inference/`）

- **对话**（✅ backends/ 5 后端）：vLLM（py313 子进程，MTP 投机 ✅on / DFlash 投机 🔶off 待 A/B）· llama.cpp GGUF · transformers VL（4B/8B，知识 LoRA v4 挂载）· remote 远程引擎 · 云端。降级链（9B 装不下→4B 诚实降级+广播）✅ 在案。
- **绘画**（✅ 三栈并存）：legacy diffusers（klein-4b，四视图旧产线）· ComfyUI klein-9b-fp8（主力，PuLID/ReferenceLatent/角色 D-LoRA）· **ComfyUI Z-Image-Turbo**（z-image 槽 + 参考图条件 + LoRA 挂载，`comfy_paint_engine.py`）。zviews 分张四视图管线 ✅（本地+云端双分支，`turnaround_cloud:"off"` 本地优先）。
- **视频**：H3 链式（`h3_chain_engine.py`+workflow json，**主力冻结令**，Turbo LoRA 6 步+sage）· 云端视频客户端 · 插件快速预览（numpy 内核，⚡按钮）。
- **语音**（✅ 三件）：SAPI5 TTS · Whisper-tiny ASR · **F5-TTS 零样本音色克隆**（`voice_clone_engine.py`，9-17 入库）。
- **视觉工具**：SAM 分割/MiDaS/YOLO/TripoSR（vision_tools）· 人脸相似度（face_similarity）· 提示词中译英（prompt_translator）。
- **加速器**：SageAttention ✅on（图像侧 +18% 实测）· T8 双时钟 🔶off（实测-3.5% 否决默认）· MTP ✅on · DFlash 🔶off（**勘误：草稿 2.58GB 已于 9-15 下载到位** `models/dflash/qwen35-9b-draft/model.safetensors`，仅开关未开）。

## 四、模型资产层（`models/` ✅ 目录实测）

对话：qwen35-9b-w4a16 / qwen3-vl-4b / qwen3-vl-8b-awq；绘画：paint/（klein 系+qwen-edit+**z-image-turbo** 三件）；视频：video_gen/h3 全套；语音：whisper-tiny；视觉：sam-vit-h/face（insightface）；embed/（bge）；LoRA：lora/+style_lora/（含 **夏沐沐 z-image comfy 版 368MB** ✅ 双位部署）；dflash/（草稿，见上）。manifest：models_manifest.json（本地运营数据）。

## 五、服务与横切层（`src/services/` ✅ 41 项）

- **队列**：统一图像队列（单 worker+位次表）· 视频队列 · 任务队列 · 对话排队。
- **插件系统**（✅ `plugin_runtime/` 10 文件+security 7 件）：OSP v1 基类/加载器（内存直读 CuteMamen 包）/登记表三闸/事件桥/沙箱+宿主（sandbox_host）/**安全盾六件**（行为指纹/意图探针/断路器/动作追踪/监控/安全检查）+security_gate。技能插座已铺 dialog/novel/comic 三端点+WS 注入（9-17 批0~4）。出厂插件：RustCoding.CuteMamen（✅ plugin/；VideoMaking 已迁包体系）。
- **训练**（三服务归一基座 ✅ 9-18 批3）：character_lora（人物 LoRA 产品化，P3 全链）/ style_lora / lora_training + training_common。
- **知识/学习**：knowledge_service（FTS5+向量+RAG 注入）/ 质量闸/体检周报/学习代理（browser_agent+browser_pool，登录墙识别）/learning_scheduler。
- **治理守卫全家桶**：resource_guard（RAM/显存三线）· quality_governor（降参）· thermal_guard · **power_guard（电源防护，9-18 新增）** · heartbeat（取证）· health_check · vram_policy+gpu_budget · scheduler 五件 · **lan_auth 中间件（LAN 鉴权，9-18 新增）**。
- **平台**：upgrade_service（拖包升级+Ed25519）· 自动备份调度 · event_log/flow_trace · web_search（免费双通道+自带 Key）· cloud_provider_service · ws_hub · page_guard。

## 六、配置门控状态（`src/config.yaml` ✅ 实值，对账易错点）

| 能力 | 门控 | 现值 | 态 |
|---|---|---|---|
| 绘画主引擎 | paint.gen_engine | comfy | ✅ 生效 |
| 四视图管线 | manga.turnaround_engine | **zviews** | ✅ 新管线为默认 |
| 四视图云端 | manga.turnaround_cloud | **off** | 🔶 显式选才走云（拍板 B） |
| H3 双时钟 | manga.h3_dual_clock | false | 🔶 实测负收益否决 |
| sage 注意力 | use_sage_attention | true | ✅ |
| H3 步数 | h3_steps | 6 | ✅ |
| vLLM sleep | vllm.sleep_mode | false | 🔶 杀进程让渡制 |
| MTP 投机 | mtp_speculative | true | ✅ |
| DFlash 投机 | dflash_speculative | **false** | 🔶 权重已就位未开闸 |

## 七、发行与授权层

打包台 packaging_console（✅ 冻结令）· 发码台 license_console（Ed25519 指纹激活）· 升级器 updater/（断电自愈）· 桌面壳 launcher（boot/shell/stop 三件+单实例守卫+页面守卫倒计时）。license_gate 端点在库。

## 八、对账要点（已知口径差与缺口，⚠️ 区）

1. **CLAUDE.md 技术栈节已过时**：路由 10→12（training 新增）、版本 2.3.1→2.5.0（CLAUDE.md 已随重构更新与否需对）、backend/ 路径→src/（全仓扁平化）。对账以本清单代码事实为准。
2. **「已建成 ≠ 已实测」清单**：DFlash（权重在、A/B 未跑）、T8 双时钟（默认关）、sleep_mode（false）、插件沙箱/安全盾（单测在、生产实弹口径待验）、power_guard/lan_auth（今日新落库、运行验证待观察）。
3. **历史悬余**：v1 全模型废件 2×24.6GB（data/lora_dataset/char_xia_mumu_real/output/，09-20 隔离区脚本处置）；审计文档《round-2026-09-16-root-cause-audit》曾长期未提交（今日批3 文档对账已收，需确认在库）。
4. **证据元数据**：单测 137 文件（最近全量口径 883 passed+9 skipped，9-16 审计实跑）；前端 vitest 71 项；四闸 ruff/pytest/tsc/vitest+mypy 基线 231 条冻结。

---
*生成：两会话监督会话（只读）；对账时若发现清单与代码不符，以代码为准并回注本文件。*
