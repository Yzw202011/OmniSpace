# OmniSpace 根查清单（2026-09-18 · 挖掘到根部 · 零文档依赖）

> **定位**：对账三部曲终章——v1 基线（功能层）→ 深查版（活体/数据/消费关系）→ **本版（断链/死根/真源核验）**。前两版见同目录。
> **方法**：全部结论出自源码解析+只读 DB 查询+磁盘实测+活体探针；不引用 docs/ 与 CLAUDE.md 的任何宣称（下文提及 CLAUDE.md 处均为「实测后反向核对其宣称是否成立」）。
> **快照**：2026-09-18 22 时段，HEAD=`f610226`。

---

## 根查①：路由挂载链 —— ✅ 全净

- 挂载机制：`src/main.py` `_API_MODULES` 显式白名单（21 项）经 importlib 动态挂载；`manga` 项经 `src/api/manga/__init__.py` 聚合 8 个子路由（storyboard/keyframe/video/voice/comic/comic_asset/comic_script/comic_export）。
- **断链核验**：顶层 20 个 api 文件 ↔ 白名单 **零死模块、零幻影项**；manga 未聚合的 3 文件（comic_gen/common/describe_refine）实测无路由装饰器（服务层，非断链）。

## 根查②：数据库 ↔ 代码 —— ✅ 结构全接通

- 36 张非 FTS 表（42−6 影子）**全部在代码中有引用，零死表**；代码引用但库中不存在的「幻影」全为跨库（flow_executions/flow_nodes→flow_trace.db、model_usage_events→model_usage.db）或正则噪音。
- **休眠表 4 张**（实存但仅 schema/系统面引用）：`director_cameras/director_characters/director_stages/scene_objects`——引用仅见 `src/data/database.py`（建表/迁移）与 `src/api/system.py`；3D 功能代码已剔除，**表在库≠功能在**，对账勿混淆。

## 根查③：配置键 ↔ 消费方 —— 🔶 22 候选，实测多为「间接消费」误报，真残留 2 类

- 方法与口径：宽松正则直查 `.get("key")` 得 22 个孤儿候选；抽查发现大量**间接消费**（例：`scheduler.thresholds` 实为 `config.py:94 THRESHOLDS=_cfg[...][...]` 模块级常量 → resource_guard.py:51-58 消费 ✅ 存活；CLAUDE.md §7「阈值真源=config.yaml」宣称经实测**成立**）。
- **真残留（实测实锤）**：①`video.ltx2_max_audio_sync_seconds`——LTX2 引擎已剔除，键仍被 config.py/model_manager 读=死功能的活配置；②`database.wal_mode`——代码硬编码 `PRAGMA journal_mode=WAL`（database.py:458），该键为装饰性（值恰好一致故无害）。
- 判读：config.yaml 顶层 18 节，绝大多数键经 config.py 常量层间接消费；孤儿判定必须过 config.py 中转层，**22 候选中抽查 4 项 3 项为误报**，真实孤儿估计个位数。

## 根查④：前端可达性 —— ✅ 全净

- 81 个组件**零孤儿**（每个组件名至少被一处 import）；router.tsx 12 路由全部指向真实文件，无断链。

## 根查⑤：模型登记 ↔ 磁盘 —— ✅ 17/17 全对上 + ⚠️ 一条新线索

- `models/models_manifest.json` 17 条目逐一对照磁盘（models/ 前缀口径）：**零缺失**。
- **⚠️ qwen3-vl-2b 线索链**（三版累积实锤）：活体 `/models` 返回它 → manifest **无**此条目 → 磁盘 models/ **无**此目录 → 代码 `dialog.py:658` 有专判（`if "qwen3-vl-2b" in m:`）。定性：**「幽灵候选」**——来源应为 model_manager 的内置种子清单（代码内硬编码候选位），非登记/磁盘/manifest 三源任何一方。**对账第一优先：确认它是故意保留的下载候选还是应清理的死条目。**

## 根查⑥：诚实边界

- pytest 收集未跑（避免与在飞会话互扰）；「孤儿键 22 候选」未逐个终核（抽查口径已注明）；活体探针仅 /models 一个 GET 样本；组件「零孤儿」为名字级判定（同名组件互引不可分辨，属方法精度上限）。

## 总评（对账口径）

| 根查项 | 结果 |
|---|---|
| 路由挂载 | ✅ 零死模块 |
| DB↔代码 | ✅ 零死表（4 张 3D 休眠表在案） |
| 配置消费 | ✅ 主体接通；2 处真残留（ltx2 键/wal_mode 装饰键） |
| 前端可达 | ✅ 零孤儿零断链 |
| 模型三源对账 | ✅ manifest↔磁盘全对；⚠️ vl-2b 幽灵候选 1 条 |

**结论**：经过 09-16~18 的连日审计修复，代码根部（挂载/建表/消费/可达性/资产三源）**未发现结构性断链**——与旧文档宣称的「API 面 40% 水分」时代相比已是两个体质。当前根部级别待处置事项仅 3 条：vl-2b 幽灵候选定性、ltx2 残留键清理、3D 休眠 4 表处置口径。

---
*生成：两会话监督会话（只读，零文档依赖）。对账入口：先核 §⑤ 的 vl-2b，再定 §③ 残留键与 §② 休眠表三件事。*
