# OmniSpace 功能能力深查清单（2026-09-18 · 零文档依赖版）

> **与 v1 基线的关系**：本文是 [功能能力清单-2026-09-18-对账基线.md](功能能力清单-2026-09-18-对账基线.md) 的深查升级——**不引用任何 docs/ 文档与 CLAUDE.md 口径**，只用四类一手证据：
> ①活体后端探针（localhost 只读 GET）②数据库只读查询 ③源码提取（正则/AST 级）④磁盘资产实测。
> **方法误报声明**：端点消费交叉核用「尾段签名法」（对参数路径 `{id}` 型有已知盲区，见 §四）。
> 快照：2026-09-18 21:2x，HEAD=`b64d176`，后端进程存活（心跳在跳）。

---

## 一、活体层（运行时探针 ✅ 实弹）

- 后端 `127.0.0.1:5800` 存活，`GET /api/v1/models` 实返标准信封+分组模型表（dialog/paint/…）。
- **`/openapi.json` 被主动关闭**（返回 404 信封 SYSTEM_RESOURCE_NOT_FOUND）——发行安全姿态，非故障；后果=路由清单只能以代码口径为准（下节）。
- ⚠️ 对账疑点：`/models` 返回中出现 `qwen3-vl-2b`（id/name/4GB/min_vram 4）条目，但 `models/` 磁盘目录清单里**无此目录**——注册表与磁盘不一致（可能：外部位/占位/待下载），**对账时优先核此项**。

## 二、数据层（SQLite 只读实测 ✅）

- 主库 `data/omnispace.db`：**42 张表**（user_version=13）。全清单：
  api_keys, art_styles, behavior_logs, char_lora_tasks, comic_assets, dialog_messages, dialog_sessions, **director_cameras, director_characters, director_stages**, keyframes, kg_edges, kg_entities, knowledge_fts(+config/content/data/docsize/idx 影子5), knowledge_meta, learning_logs, learning_sessions, learning_settings, learning_topics, model_benchmarks, models, novel_chapters, novel_characters, novel_forewords(novel_foreshadows), novel_outlines, novel_projects, paint_history, projects, **scene_objects**, schedule_history, storyboard_rows, storyboards, style_tasks, system_settings, train_tasks, video_tasks, voice_profiles
- 独立库：`logs/flow_trace.db` 2 表（flow_executions/flow_nodes）· `data/model_usage.db` 1 表（model_usage_events）。
- ⚠️ **3D 遗留表在库**：director_×3 + scene_objects——3D 导演台代码已于 2026-08-29 整链剔除，但这 4 张表仍留在生产库中（无害残留，对账时应知：表存在 ≠ 功能存在）。

## 三、API 层（源码提取口径）

- 端点总数 **355**（正则提取 `@router.(get|post|put|delete)` + APIRouter prefix；与逐文件装饰器计数 357 差 ±2=多行装饰器形态，两口径并存记录）。4 个 WebSocket（/ws、/dialog/stream/{id} 废弃兼容、/hardware/realtime、/learn/session/progress）。
- 分模块端点数（✅ 计数）：system 38 · novel 28 · models 28 · knowledge 27 · draw 24 · style 23 · dialog 22 · comic_asset 21 · learning 16 · storyboard 14 · comic 13 · logs 13 · video 12 · learn 12 · plugins 11 · cloud 10 · keyframe 7 · browser 7 · upgrade 6 · voice(manga) 6 · hardware 6 · character_lora 6 · license 2 · 其余各 1。

## 四、端点 ↔ 消费方交叉核（本次深查新增，✅ 机械全量）

方法：355 端点逐一取「去参数尾段签名」，在 frontend/src（.ts/.tsx）与 tools+scripts+tests（.py）语料中匹配。结果：

| 分类 | 数量 | 说明 |
|---|---|---|
| 前端消费 | **331（93%）** | UI 有调用方 |
| 仅测试/工具消费 | 16 | 如 draw/inpaint、draw/upscale、style resume-training、models/predict——功能在但 UI 无入口 |
| 无任何匹配 | 8 | **全部为参数路径型**（models/{model_id}×2、plugins/{name}、style/{version}×2、system/apikeys×3）——前端用模板字符串拼接，签名法天然盲区，**判定为疑似误报；真实死端点估计 0~8 个，需人工终核** |

**结论**：旧时代「API 面 40% 水分」的口径（38 死端点+112 仅测试）已被 09-16~17 的入口补齐工程实质压缩——当前死端点风险收敛到个位数候选。

## 五、引擎/资产/门控（代码+磁盘实测，✅ 沿用 v1 已核事实，剔除文档引注）

- **对话 5 后端**（src/services/inference/backends/：vllm/llama/transformers/remote/gguf）· **绘画 3 栈**（paint_engine 旧栈/comfy_paint_engine 主力含 Z-Image 工作流族/云端）· **视频 2+1**（h3_chain_engine+cloud_video_client+插件预览）· **语音 3**（voice_engine/whisper/voice_clone_engine F5-TTS）· 视觉 5（segment/face_similarity/…）· 翻译（prompt_translator）。
- **门控实值**（src/config.yaml 直读）：zviews=默认管线 ✅ · turnaround_cloud=off 🔶 · h3_dual_clock=false 🔶 · sage=on ✅ · MTP=on ✅ · DFlash=false 🔶（**草稿权重 2.58GB 已在盘** models/dflash/qwen35-9b-draft/model.safetensors ✅ 实测——纠正此前「未下载」传闻）· sleep_mode=false。
- **模型资产**（models/ 实测）：对话 3 目录 · paint/（klein 系+qwen-edit+z-image-turbo）· video_gen/h3 全套 · whisper-tiny · sam-vit-h+face · embed · LoRA 两区（含夏沐沐 z-image 368MB 双位硬链）。
- **插件系统**（src/services/plugin_runtime/ 实测 10 文件+security/ 7 件）：OSP v1+加载器+登记表+事件桥+沙箱双件+安全盾六件+security_gate；出厂包 RustCoding.CuteMamen（plugin/ 实测在盘）。
- **横切 41 服务**（src/services/ 目录实测）：四队列 · 训练三服务+公共基座 · 知识/学习/浏览器代理 · 治理守卫（resource/quality/thermal/power/heartbeat/health/vram/scheduler/lan_auth）· 升级/备份/事件流/WS hub。
- **前端**：12 路由（router.tsx 直读，training 为新增）· 81 组件 · 12 store。

## 六、诚实边界（本次深查**没有**做的事）

1. 未跑测试套件（137 个测试文件在盘，未实弹——避免干扰在飞会话与 GPU）。
2. 未逐端点实弹（仅 /models 一个 GET 样本）；端点级功能正确性不在本清单承诺内。
3. 消费交叉核的 8 个候选需人工终核（模板字符串盲区）；16 个「仅测试」端点的 UI 缺口是否属故意（如运维端点）未逐个判读。
4. DB 只看结构未看数据质量（行数/空表未查）。

---
*生成：监督会话（只读）。对账优先核三条：①qwen3-vl-2b 注册表 vs 磁盘 ②8 个死端点候选人工终核 ③3D 遗留 4 表处置口径。*
