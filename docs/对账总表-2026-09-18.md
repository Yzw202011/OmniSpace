# OmniSpace 对账总表（2026-09-18 · 四轮盘点合并版）

> **定位**：监督会话四轮只读盘点的**唯一汇总入口**——①功能层基线（v1）②深查版（活体/DB/消费关系）③根查版（断链/死根）④本轮补充（启动链/依赖根）。明细证据见同目录三份分册，本表只留结论与对账行动项。
> **全程零文档依赖**：只用源码解析、只读 DB 查询、磁盘实测、活体探针（localhost GET）与监督期间亲历的运行日志。
> **快照**：2026-09-18 22:3x · HEAD=`f610226` · 后端存活。

---

## 一、规模底数（对账用硬数字）

| 层 | 数字 |
|---|---|
| 前端 | 12 路由 · 13 组件域 · **81 组件（零孤儿）** · 12 store |
| 后端 API | **355 端点 / 30 模块**（挂载白名单 21 全命中，零死模块）· 4 WS |
| 引擎 | 对话 5 后端 · 绘画 3 栈（legacy/comfy-klein/z-image）· 视频 2+预览 · 语音 3（SAPI/Whisper/**F5-TTS 克隆**）· 视觉 5 |
| 数据 | 主库 42 表（36 实体+6 FTS 影子，**零死表**）· flow 库 2 · 用量库 1 · user_version=13 |
| 横切 | 41 服务（四队列/插件系统 10+安全盾 7/训练 3/守卫 9/平台 8…） |
| 模型 | manifest 17 条 **↔ 磁盘 17/17 全对** · 门控 9 键实值在案 |
| 测试 | 137 个单测文件（未实跑，避让在飞会话） |

## 二、健康结论（根部体检）

**挂载链/建表链/配置消费链/前端可达链/模型三源——五链全通，无结构性断链。** 端点消费率 93%（331/355 前端有入口，16 仅测试工具，8 无匹配候选全为参数路径型疑似误报）。启动链经代码锚点+监督期间 5 次亲历启动序列核验（scheduler/resource_sampler/page_guard/heartbeat 显式启动 + 日志实证的 flow_trace/periodic/browser/encoder/model_manager 等 ~20 项初始化）。

## 三、待对账清单（合并去重后 **8 项**，按优先级）

| # | 事项 | 证据位 | 定性 |
|---|---|---|---|
| 1 | **f5_tts 依赖未钉版未安装**——音色克隆（9-17 入库功能）主运行时 import 失败=码在依赖缺的休眠态；出包若按 lock 组装必缺此功能 | voice_clone_engine.py:31,45 懒加载；requirements-lock 无 f5_tts；py312 实测不可导入 | 🔴 功能级缺口 |
| 2 | **qwen3-vl-2b 幽灵候选**——/models 返回+dialog.py:658 专判，但 manifest 无/磁盘无 | 活体探针+三源交叉 | 🟡 待定性（下载候选位 or 死条目） |
| 3 | **8 个死端点候选终核**——全为 `{id}` 参数路径型，签名法盲区，需人工确认前端模板字符串调用 | 深查版 §四 | 🟡 预计误报居多 |
| 4 | **16 个仅测试/工具消费端点**——UI 无入口（draw/inpaint、draw/upscale、style resume-training 等），是否故意留运维口 | 深查版 §四 | 🟡 口径确认 |
| 5 | **可选依赖未钉版**——lz4（运行时日志实证缺席并优雅降级）/reportlab/soundfile/basicsr/redis | 依赖交叉 §④ | 🟡 分发口径 |
| 6 | **ltx2 残留键**——死功能的活配置（被读但引擎已剔除） | config.py/model_manager | 🟢 卫生 |
| 7 | **3D 休眠 4 表**——director_×3+scene_objects 仅 schema 层引用，留或清待拍板 | database.py/system.py | 🟢 口径 |
| 8 | **盘点文档处置**——本总表+v1/深查/根查三册均在 docs/ 未跟踪，入库或留置待令 | docs/ | 🟢 流程 |

## 四、诚实边界（对账时必须随身携带的口径）

1. 测试套件未实跑；端点级功能正确性未逐个实弹（活体样本仅 /models 一个 GET）。
2. 消费交叉用「尾段签名法」，对模板字符串/封装调用有已知盲区（§三.3 即此盲区产物）。
3. 组件「零孤儿」为名字级判定；依赖对账为顶层名粗口径（别名已解 PIL→pillow、fitz→pymupdf、docx→python-docx 均在 lock）。
4. DB 只查结构未查数据质量（行数/空表）。
5. 本表由监督会话只读生成；与两会话在飞工作零互扰。

---
*对账顺序建议：#1（功能缺口）→ #2（幽灵候选）→ 其余口径项随手清。*
