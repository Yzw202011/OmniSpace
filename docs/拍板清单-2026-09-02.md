# 拍板清单 — 2026-09-02 凌晨整理后遗留决策项

> 背景：08-31 体积计划第 1 级（60G）+ 09-02 凌晨 ComfyUI/开发残留第二批（~223M）均已移入隔离区 `E:\_trash_staging\2026-09-01\`（同盘移动未删，MANIFEST.md 有完整清单与恢复命令）。**以下是全部还没动手、等你拍板的事项**，每项带建议。

## A. 磁盘大头（模型/构建，按收益排序）

### A1. 隔离区清空 → 释放 ~60G 【建议：观察 7 天后清空】
9-01 凌晨整理的全部内容已在隔离区。截至目前验证全绿（审计/后端/绘画/知识库）。你确认近期没发现任何功能异常后，说一声即真空删，E 盘可用 104G → ~164G。

### A2. ComfyUI 侧 H3 冗余量化三件 49.2G 【建议：搬隔离区（后端零引用）】
| 文件 | 体积 | 后端活链 | 手动模板 |
|---|---|---|---|
| text_encoders/qwen3vl_32b_minimax_h3_nvfp4_awq.safetensors | 15.7G | 零引用（活链用 int4_convrot） | 引用 50 处 |
| diffusion_models/minimax_h3_fl2va_pruned_int8_convrot.safetensors | 21G | 零引用（链式用 int8-ref2va、单发用 nvfp4-fl2va） | 引用 2 处 |
| h3/MiniMax_H3_Ref2VA_pruned_nvfp4.safetensors（注意：与 ComfyUI 侧硬链接同源，两端一起才算数） | 12.5G | 零引用（链式用 int8-ref2va） | 引用 5 处 |

**重要关联发现（09-02）**：引用它们的手动模板里有 3 个模板本身已残废——引用了不存在的 `minimax_h3_ref2va_pruned_fp8_scaled.safetensors`（21 处）和两个不存在的 upscaler lora。即：**部分"在用"其实是死引用**，真要手动跑也会报错。若你已不再手动跑旧模板 → 这 49.2G 可全搬。

### A3. dist_out 两个完整发行包 38G 【建议：保 protected、plain 搬隔离区】
`OmniSpace-2.3.1-gd938ce9/`（19G，09-01 19:29）+ `OmniSpace-2.3.1-protected/`（19G，09-01 19:41，防破解版）。同一版本两次构建，可由金母版重建。封装发行 v5（setup.exe）还要多轮构建，建议：protected（含六道锁）为基线保留，plain 搬隔离区；等 v5 落地再统一清。

### A4. bge-m3 整目录 8.0G 【建议：搬隔离区（代码零引用）】
检索引擎硬编码用 `embed/bge-large-zh`（vector_db.py:29），backend/skills/scripts 全库无 bge-m3 加载点，仅文档提及。08-31 计划写"OmniLearn 在用"与代码不符。上轮已搬走它的半成品和 bin（5.8G），剩下的 2.2G safetensors 本体是最后的冗余。

### A5. 第 3 级远期 49.8G 【建议：暂不动，等路线拍板】
- klein-4b 整目录 15.2G：9b 已回补且在白名单，4b 仅 one-pass 四视图代码引用，确认放弃该路线即可删。
- klein-9b diffusers 分片 18.2G + text_encoder 16.4G：推理实际走 GGUF Q6_K，但**就绪检查硬要求分片**（paint_engine.py:206-224），删=先改代码+回归。

## B. ComfyUI 侧小额项

### B1. 六个零使用节点包 ~53M 【建议：搬隔离区】
Easy-Use（33M）、Theodore_Director（9.5M）、rgthree（7.4M）、FlowDirector（2M）、Custom-Scripts（0.7M）、Scale-Image（0.04M）。活链构建脚本和手动模板的节点类双向比对均零命中。装回容易（ComfyUI-Manager）。
**明确保留**：mixlab 211M（模板在用 TextSplitByDelimiter）、Impact/KJNodes/VHS/Motion-Context×2/三个 upscaler 包（模板在用）、Contex-Loop/h3-turbo/nvidia_rtx/PuLID/Manager/中文翻译包（活链或 UX 必需）。

### B2. 三个只被旧模板引用的 turbo lora 4.1G 【跟 A2 绑定拍板】
lightx2v_4step（301M）+ fl2v_8step（1.9G）+ ref2v_4step（1.9G）。活链只用 turbo_v4_step600（744M）。旧模板若判死刑，这三个一起走。

### B3. 残废手动模板处置 【建议：删除，需要时用 build 脚本重生成】
`user/default/workflows/` 里引用不存在文件的模板（fp8_scaled、upscaler lora 引用者）。构建脚本（tools/build_manga_*.py）是真源，可随时重出。

### B4. tools/git 便携 git 91M 【建议：搬隔离区】
launcher/backend/license_console/tools 全库零引用；系统 Git 在用。若你有时手动用它跑 git 命令则保留。

## C. 数据保留策略（不急，但迟早要定）

### C1. data/backups 413M 【建议：设保留份数，如最近 3 份】
数据库自动备份累积。可加自动轮转（日志机制已有 10M×5 先例）。

### C2. data/keyframes 652M + data/generated 425M 【建议：按项目归档或定期清】
历史分镜帧/生成图（含大量 08 月测试产物）。属用户内容，我不动；如果早期测试项目已不要，可按项目删。

## D. 代码级死重（TripoSR 同类排查，09-02 下午追加）

> 排查口径：**产品链路可达性**——「模型管理页可手动加载」≠ 在用。与 A/B 组（在用模型的冗余格式）互补，不重复。每项均带代码证据。

### D1. 大额死重（合计 ~30G，拍板后搬隔离区）

**D1a. codestral-22b 13.3G【建议：搬——除非未来要做代码助手功能】**
dialog 类登记，但 DIALOG_ROUTING_TABLE、module-config 默认槽、任何功能链**零引用**；全库引用仅 2 处：分类器标签（classifier.py:101）与模型管理页文案（ModelManager.tsx:149/207「专职程序员」描述）。唯一通路 = 用户在模型管理页手动加载它当对话模型——无任何功能自动走到它。

**D1b. AI 语音栈四套 16.6G【建议：先拍语音技术路线，保留中选一家，其余搬】**

| 目录 | du 实占 | 门控证据 |
|---|---|---|
| cosyvoice2/ | 5.3G | 官方代码包未安装（pydeps 无 cosyvoice），voice_engine.py:157 明确「已门控」 |
| qwen3-asr/ | 4.4G | 语音引擎**零引用**（全库 grep 仅剩注释与排除清单） |
| qwen3-tts/ | 4.3G | 语音引擎**零引用**；dialog.py:508 明确把它排除出对话模型清单 |
| gpt-sovits/ | 2.6G | F-08 权重随包但官方推理代码包未装（voice_engine.py:71-81，注释明言「绝不伪造 AI 合成」） |

产品实际发声 = **Windows SAPI5 系统语音**（comtypes COM）+ whisper-tiny 149M 真实转写（transformers 原生，**保留**）。注意定性：这四套是**功能欠账**（F-08 声音克隆、README 路线图「语音克隆与 TTS 优化」），不是意外下载——若路线继续做，建议只留选定引擎那一份。

### D2. 小额死重（合计 ~2.9G，建议直接搬）

- **TripoSR 1.6G + MiDaS 0.15G + YOLOv8 6M**：/art 视觉端点前端零调用（本次排查起点）；**SAM ViT-H 2.4G 保留**——漫剧 comic_gen.py:137 实际引用。配套 /art 端点可一并下线。
- **PuLID 1.0G**（paint/pulid）：自动链默认旁路（KF1 构建脚本注释），仅手动 ComfyUI 实验在用（08-29 竞品对齐实验用过）——与 B3 手动模板处置**联动拍板**。⚠️ 已验证：comfy_model_map.json:15 有其硬链映射，搬走时须同步删该映射行，否则 comfy_link --check 报悬空。
- **hf_cache 91M**：all-MiniLM 自动下载缓存，唯一"引用"是 model_registry.py:36 的注释。

### D3. 代码卫生（≈0 体积，随手清）

- **manifest 失真**：qwen3-vl-8b 登记 16.5G、磁盘实为 **4KB 空壳**——16G 显存档路由陷阱（DIALOG_ROUTING_TABLE 16G 档首选它，必空转回退 awq 档）。建议删空目录并修 manifest（或补权重二选一）。
- backend 根 `_planA*.py`/`_repro.py` ×6 一次性调试脚本；tools/_tmp_* ×2。
- frontend：libs/ 3.9M（react.min/three.min/GLTFLoader/OrbitControls/babel——mock 时代遗产；已验证 index.html 零引用、make_dist 白名单只带 frontend/dist 不带它，删之安全）、js/ 与 assets/ 空目录、three+@types/three 依赖（⚠️ 已验证 vite.config.ts 三处引用 @three 别名/manualChunks three-vendor/optimizeDeps 预打包 + tsconfig @three 路径——**删依赖必须连这四处一起改**再 pnpm install+重建，只删 package.json 会破 dev/build）、common/Toast.tsx 未引用、manga/steps/ 与 src/pages/ 空目录、pnpm-workspace.yaml+package-lock.json 双锁并存（package-lock 无消费方——构建走 pnpm+便携 Node，可删）。
- **旧别名端点族前端零调用**：/paint×12、/storyboard×8、/dialog×4、/art×7、/learn/analysis×4——测试零依赖，可删。⚠️ 两个例外：/voice×4 **E2E 在用**（tests/flow/cases_chat.py:473 调 /voice/status，删须同步改测试）；/video 别名**产品在用**（绘画页视频面板走 /video/generate），别动。
- ~~director_* 三张死表~~ **撤回（09-02 晚验证）**：api/system.py:495-504 的项目导出/备份仍在读这三张表——不是死表，**不能直接 DROP**；要清得先改导出代码，低优先搁置。
- DB models 表 13 行幽灵种子数据（qwen3-vl-2b/flux.1-dev/kolors-2.1/sdxl-lcm/wan2.1-14b/cogvideox 等——盘上无目录却标 ready；已验证 `_merged_models()` 只合并路由表∪磁盘扫描、**不读此表**，无产品影响，纯数据卫生，可选清洗）。

### 明确不在死重之列（防误删备忘）

- SD 1.5 剩余 5.2G：AnimateLCM 视频兜底链底座（video_engine.py:74-80 探测路径）。
- SDXL fp16 余量：学习页 LoRA 训练底座选项（api/learn.py:68）+ 引擎候选（gen_router 注释明示不入风格路由但保留）。
- Klein-9B FP8 9G：ComfyUI 侧硬链在用（体积计划已注）。
- deepseek-r1-14b 9.3G：分镜 ai-describe/对话链 12 文件引用，在用。
- wan2.1-i2v / vace / ltx：引擎管线在 + 模型下拉可选（弱活，砍它们需连代码一起动，不建议本轮碰）。
- whisper-tiny / SAM / DINOv2 / InsightFace / bge-large-zh：一致性链与检索在用。

### D 组影响面验证结论（09-02 晚逐项追码，清理前必读）

| 项 | 判定 | 依据 |
|---|---|---|
| D1a codestral | ✅ 可搬 | selector 路由表无它（data/models.py:119-123）；DB module_config 实值 allowed 列表无它（dialog=[4b/8b/8b-awq]）；仅模型管理页手动列表少一项 |
| D1b 语音四套 | ✅ 可搬（行为零变化） | 四套本来全部门控不参与推理（产品=SAPI5+whisper-tiny）；克隆端点 voices_clone 缺代码包时如实报 VOICE_CLONE_UNAVAILABLE（manga/voice.py:284-290），删权重只是换了报错文案 |
| D2 TripoSR/MiDaS/YOLO | ✅ 可搬 | 引擎全部按需懒加载，缺权重走 ApiError 降级；/art 端点建议连带下线（前端+E2E 双零依赖） |
| D2 PuLID | ✅ 可搬（带条件） | 同步删 comfy_model_map.json 映射行；ComfyUI 侧硬链因 inode 语义仍在，但映射审计会报悬空 |
| D2 hf_cache | ✅ 可搬 | 纯下载缓存，无加载路径 |
| D3 manifest 空壳修正 | ✅ 安全 | selector 第一遍跳过未下载、回退 awq 档（selector.py:150-191 双遍回退实测读码确认） |
| D3 杂件（_planA/libs/Toast/空目录/双锁） | ✅ 可搬 | index.html 零引用 libs；make_dist 只带 frontend/dist；package-lock 无消费方 |
| D3 /paint、/storyboard、/dialog、/art 别名 | ✅ 可删 | 前端+flow E2E+契约测试三方零依赖 |
| D3 /voice 别名 | ⚠️ 先改测试再删 | cases_chat.py:473 在调 /voice/status |
| D3 three 依赖 | ⚠️ 四处配套改 | vite.config 三处 + tsconfig @three，只删 package.json 会破 dev/build |
| D3 director_* 表 | ❌ 撤回不删 | system.py:495-504 项目导出/备份仍在读 |

**清理后必做三件事**（无论搬哪几项）：
1. **重录金母版基线**：golden_master.py 探针含 `GET /api/v1/models`（golden_master.py:46），搬任何权重都会让 verify 变红——属预期，`record` 重录即恢复；
2. 跑 `tools/run_tests.py`（L1 冒烟+单测）回归；
3. 同步修订 `models/models_manifest.json`（被搬条目改状态或删除）与 comfy_model_map.json。

## E. 第二批盲区（pydeps 依赖 / 遗留 DB / tests 归档 / 隐藏区，09-02 晚）

> 诚实声明：E1 前两轮分析作废重做——第一轮拿「包名」grep 代码（Python 导入用模块名，方法错误，170 包全部误判零引用）；第二轮依赖闭包脚本有 bug（把 chromadb/cryptography/pynvml 等在用包误判为孤儿）。**最终结论以三重证据定案：逐包 import grep（宽松匹配含函数内动态导入）+ dist-info「被谁依赖」反查 + 体积实测**。

### E1. pydeps 依赖分析（发行包瘦身依据）

**真孤儿（运行时+开发脚本双向零 import、无人声明依赖，可卸或发行剔除）**

| 包 | 体积 | 证据 |
|---|---|---|
| pyttsx3 | 0.1M | 语音走 comtypes 直连 SAPI5，requirements.txt 列了但代码零引用（requirements 条目同步删） |
| loguru + win32-setctime | 0.4M | 日志走标准库 logging（middleware/logger.py），零引用；win32-setctime 是它的依赖 |
| RestrictedPython | 0.1M | requirements 注释写「安全沙箱」但浏览器代理走 Playwright，零引用 |
| grpcio-status / hf-xet / kernels×2 | ~4M | 无人声明依赖；hf-xet 是 HF 下载加速、kernels 是 diffusers GPU kernel 惰性加载——**谨慎保留**（删除可能拖慢下载/首次推理，收益 4M 不值） |

**在用但极易误判的（防误删备忘，动态 `_try_import` 加载）**：llama_cpp（GGUF 对话链，gguf_backend.py:33）、ultralytics+thop 13.3M（YOLOv8，detect_engine.py:39——**若 D2 删 YOLO 则同步卸**）、trimesh（TripoSR 导出 glb——**绑 D2**）、**fitz/pymupdf 51.1M（知识库 PDF 导入命脉，knowledge.py:103，绝不剔）**、qwen-vl-utils（transformers Qwen3-VL 运行时需要，0.1M 保留）。

**chromadb 遗留依赖树（发行剔除候选 ~125M）**：kubernetes 69M + oauthlib/requests-oauthlib/durationpy（chromadb 声明，但单机模式只在其 distributed segment 才 import）、opentelemetry 六件 15.6M（chromadb 遥测 export，单机不触发）、modelscope 38M（运行时零引用，唯一消费方是 tools/download_klein9b 脚本）、typer 0.8M。**正确姿势：不卸载、改发行过滤器**——make_dist.py 已有 `_pydeps_filter` 钩子（make_dist.py:55），往里加排除清单即可「开发目录保留、发行包剔除」；剔除 kubernetes/otel 前先实跑一次知识库导入确认 chromadb 不炸，然后重录金母版。

### E2. data/ 遗留 DB 五件（~900K，搬隔离区）

| 文件 | 定性 | 依据 |
|---|---|---|
| preference.db 20K | 零引用 | 全库 grep 无引用（08-01 遗留） |
| vector_fallback.db 12K | 零引用 | TF-IDF 降级是内存实现，无此文件读写 |
| test_smoke.db 204K | 旧冒烟产物 | 已被 backend/tests/test_api_smoke 取代 |
| omnispace.db.legacy-* ×2 650K | 历史隔离备份 | 08-02/08-04 的 .corrupt 处置残留 |
| ~~model_usage.db 16K~~ | **保留（在用）** | predictor.py:86 功能切换预测事件流，勿动 |

姿势：直接移走即可（无进程打开）；主库 omnispace.db/WAL 不碰。

### E3. tests/ 遗留脚本归档（git mv 到 tests/archive/）

- **归档**：smoke_batch1-6.py（README 已注明被 test_api_smoke 取代）、_probe_comic/ctx/ctx2/ctx3/tabs.py、_dump_routes.py、_routes.txt、_cleanup_smoke.py、_tsr_selftest.py、ui_chat008_009_046_retest.py、ui_chat012_retest.py、ui_comic069_sys020_retest.py、ui_fix_verify.py、tmp/、e2e_results.json、strict_results.json（旧结果）。
- **保留**：flow/、golden/、e2e_realmachine.py、strict_test.py、ui_batch_*.py ×4（现行 UI 冒烟批次）、README.md。
- 姿势：`git mv` 保留历史；tools/run_tests.py 只调 tests.flow.run，不受影响；归档后跑一次 `run_tests.py --smoke` 确认收集路径无变化。

### E4. 根目录隐藏区

| 目录 | 体积 | 定性 |
|---|---|---|
| .mimosa | **420M** | 安全扫描工具（Mimosa）本地审计证据库，本轮会话仍在写入。**工具数据我不动**；若工具支持只留最近轮次可清旧，否则整体搬隔离区前先确认工具可重建 |
| .cache | 104M | triton+vLLM 编译缓存——删了下次启动要重编译（慢）。默认**保留**；搬走=接受一次重编译代价 |
| ffmpeg/（根目录） | 0 | 空壳目录（真身在 runtime/ffmpeg）——删；姿势：同步更新 scripts/dir_audit.py 与 tools/make_dist.py 两处白名单 |
| logs/ | 40M | **健康不动**：事件 30 天自清、日志 10M×5 轮转、flow_trace.db 8.4M 属观测在用 |
| keys/ .trae/ .zcode/ .pytest_cache/ .ruff_cache/ | <50K | 发行密钥/IDE/自愈缓存，不动 |

### 盲区覆盖矩阵（无遗漏自证）

| 磁盘区域 | 处置 | 落点 |
|---|---|---|
| models/（293G） | 已逐族定性 | 体积计划 + A4/A5 + D1/D2 |
| tools/ComfyUI（在用部分+冗余） | 已颗粒核查 | A2/B1/B2/B3 |
| dist_out/（D:\ccd 迁移后残留） | 已列 | A3 |
| pydeps/ | 本轮 | E1 |
| runtime/ 双 Python + node + ffmpeg | 保留（vLLM 必需/构建必需） | 体积计划「工程与运行时」 |
| data/ | 已列 | C1/C2 + E2 |
| logs/ | 健康不动 | E4 |
| tests/ + backend 根脚本 + tools 杂项 | 已列 | E3 + D3 + B4 |
| frontend/（libs/three/别名） | 已列 | D3 |
| skills/（_extracted+压缩包） | 已列 | D3 |
| 根目录隐藏区 + bat/exe | 本轮 | E4 |
| keys/ build_tools/ license_console/ docs/ 采集素材/ | **明确不动**（发行密钥/构建工具链/内部工具/文档史/用户素材） | — |


## 已完成不用再管（备忘）
- 第 1 级 60G + 第二批 223M 已入隔离区（A1 清空即释放）。
- vace UMT5 已挂 wan22 硬链（省 11.4G，双路径可用）。
- runtime/node20 旧版、MinGit.zip、py310 安装 zip、ComfyUI input 残留/旧日志、dist_out 残壳——已搬（第二批）。
- 全功能体检 106 接口零故障；klein-9b 绘画 9.7s 出图；ComfyUI 治理链拉起/让渡/回收实测正常。

## 09-02 晚执行记录（"开工"指令，隔离区 E:\_trash_staging\2026-09-02\，MANIFEST.md 有逐条恢复命令）

**已执行**：A2（49.2G 三件+双端硬链）、A4（bge-m3 残余 2.2G）、B1（六节点包）、B2（三 turbo lora）、B4（tools/git）、B3（残废模板——勘察发现凌晨已清，无事可做）、D1a（codestral 13G）、D1b（语音四套 16.6G **全部入隔离区，语音路线拍板后从中恢复一家**）、D2（TripoSR/MiDaS/YOLO/PuLID 双端/hf_cache/8b 空壳）、D3 卫生件（backend 根脚本、frontend libs/Toast/双锁/空目录、manifest 剔 12 条、路由表 8b 死行摘除+两档位改指 awq）、E2（遗留 DB 五件）、E3（tests 26 脚本 git mv 归档）、E4（ffmpeg 空壳+白名单）、E1 部分（requirements 三幽灵、make_dist 发行剔除集 ~110M/包，chromadb 拦截导入逐项实测：opentelemetry 必须保留）。
**验证**：dir_audit 复跑干净 ✓｜离线冒烟 67 passed ✓（含校准后的注册表三用例）｜金母版重录+复核一致 ✓｜活后端 /models 九个被搬模型全部消失 ✓。
**遗留待办**：①A1 真空删（两批合计 144G，观察期后一声令下）；②代码链三件需重启窗口——three 四处配套删、/paint+/storyboard+/dialog+/art 别名端点删（/voice 需先改 cases_chat.py:473）、后端重启生效 data/models.py 改动；③A5 远期 49.8G、C1/C2 数据策略、D1b 语音路线定案。附注：df 中 +26G 为 pagefile.sys 系统换页临时膨胀（02:30 后端重启所致，重启机器自回落），非项目数据。
