# OmniSpace 收尾与后续路线计划 · 2026-09-02

> **诚信原则**：本文每个论断标注验证状态——✅ 已实测 / 🔶 待实测 / ⚠️ 未验证假设。
> 不可逆动作单独标红；每阶段给出回滚路径；估时是估时，不是承诺。
> 配套文档：拍板清单-2026-09-02.md（决策项详情与证据）、隔离区两份 MANIFEST.md（恢复命令）。

---

## 一页总览

```
阶段一 清理收尾（今天→约09-08，零停机）   → 观察 + 小尾巴 + A1 真空删（144G）
阶段二 重启窗口（您约时间，约30-60分钟）  → 代码链三件一次上车 + 全链验证
阶段三 决策队列（不阻塞上面，按您节奏）   → 7 项拍板，每项带建议与代价
阶段四 主线推进（拍板后启动）             → 发行包实测 → v5 安装体验（6~7周）
```

当前系统状态（✅ 2026-09-02 深夜实测）：后端运行健康、冒烟 67/67 绿、目录审计干净、金母版基线已重录一致、隔离区两批 144G 未删除可整建回滚。

---

## 阶段一：清理收尾（零停机，风险最低）

### 1.1 观察期协议（09-03 ~ 09-08，六天）✅ 协议已建立

- **您要做的**：正常使用核心功能即可（对话 / 绘画 / 分镜生图 / 视频生成 / 知识库导入）。特别留意：知识库 PDF 导入（fitz 链路）、GGUF 对话（llama_cpp 链路）——这两条是依赖分析里唯一靠动态导入保下来的，值得日常顺手点一次。
- **我方每日**：冒烟 + dir_audit 复跑（两条命令，约 1 分钟）。
- **出异常怎么办**：隔离区 MANIFEST.md 逐条 mv 反向命令，单文件级回滚（同盘瞬时）。

### 1.2 A1 真空删（预计 09-08 前后，需您一句话）🔴 不可逆

删前检查单（四条全过才动手）：
1. 六天观察期无异常反馈；
2. 冒烟 + dir_audit 当日复跑绿；
3. 您确认「最近没觉得少了什么功能」；
4. 删除范围仅 `E:\_trash_staging\2026-09-01\` 与 `2026-09-02\` 两个目录，逐目录 `rm -rf` 前 `du` 留档。

预期收益：E 盘可用 114G → **~258G**（✅ 按已搬体积算术，非预估）。
回滚路径：🔴 无（这就是为什么有检查单和六天观察）。唯一兜底：所有大件原始来源可重下（H3/模型官方源），klein-9b 分片有 ModelScope 回补脚本先例。

### 1.3 小尾巴（随手，不急）

- `.mimosa` 420M：⚠️ 工具方数据，我无权定夺——建议您问一句 Mimosa 工具方能否清旧轮次，或确认它自重建后整体搬走；
- `.cache` 104M：✅ 保留（triton/vLLM 编译缓存，删=下次启动重编译几分钟，不值）；
- git 索引里有一批**本会话之外**的暂存改动（backend 多文件 MM 状态 + .mimosa 条目）——⚠️ 来源不明（可能是您或某工具 git add 过），我没动它。建议下次提交前 `git status` 亲自过目，需要我帮忙梳理说一声。

---

## 阶段二：重启窗口（一次停机搞定代码链三件）

**为什么必须停机**：三件事分别要后端重启（新代码生效）和前端重建（dist 替换）。合并成一个窗口，只疼一次。

### 2.1 窗口前准备（不停机就能做完，我先做好待命）

| 项 | 内容 | 预检 |
|---|---|---|
| three 退场 | vite.config 三处 + tsconfig @three 一处（合计四处）配套删，pnpm install 裁掉 three/@types/three | tsc --noEmit + vite build 预构建 ✅ 可先在旁路验证 |
| 别名端点退场 | 删 /paint×12、/storyboard×8、/dialog×4、/art 六端点（vision_tools 模块下线）；/video 别名**保留**（绘画页视频面板在用 ✅ 实测） | ruff + 离线 pytest |
| /voice 配套 | cases_chat.py:473 一行改（删 /voice×4 端点的前提） | pytest 收集路径 |
| 档位表生效 | data/models.py 已改好（8b 死行摘除、两档位指 awq）✅ 离线测试已绿，只等重启上车 | 已完成 |

### 2.2 窗口内执行序列（估 30-60 分钟，⚠️ 估时非承诺）

1. 走正规链停后端（停止OmniSpace.bat，绝不直接杀）；
2. three：pnpm install → tsc+vite build → dist 产物核对；
3. 起后端（boot.py 链，vLLM 预热可跳过省 2-3 分钟）；
4. 验证序列：/health → 冒烟 → 金母版重录 → E2E 定点（对话+绘画+漫剧各一条）→ 真浏览器过一遍九页面；
5. 出问题回滚：git checkout 改动文件 + 重回旧 dist（构建产物我会留备份）。

**诚实提醒**：窗口内正在用的页面会断开，dist 替换后旧页面需刷新；vLLM 冷启约 157s 属正常等待。

---

## 阶段三：决策队列（7 项，不阻塞阶段一二）

| # | 决策 | 我的建议 | 不决定的代价 |
|---|------|---------|-------------|
| 1 | **语音路线留哪家**（qwen3-tts / cosyvoice2 / gpt-sovits / 都不要） | ⚠️ 诚实说：四家我一家都没实测过音质（全门控没跑过）。建议先花一次小实测（装官方包试跑 1-2 家，半天）再拍；不想花时间就先都不恢复，SAPI5 顶着，16.6G 继续躺隔离区 | 16.6G 在隔离区占着，A1 真空删前必须给说法（恢复一家 or 随批删除） |
| 2 | **A5 klein-4b 路线** | 建议：确认放弃 one-pass 四视图路线 → 15.2G 随批删；9b 分片 34.6G 想删得先改就绪检查代码+GPU 回归（半天+一次真跑），建议排到发行前 | 49.8G 动不了 |
| 3 | **C1 备份轮转策略** | 建议：保留最近 3 份 + 自动轮转（复用日志 10M×5 先例，代码半天） | data/backups 无限涨 |
| 4 | **C2 历史生成物** | 您的内容我不动；早期测试项目不要了就按项目删 | ~1G 恒定占用 |
| 5 | **页面关闭全链退出** | 方案已出待拍三项（排空策略/倒数 60s/开关）——产品体验项，建议排发行前定 | 无（不影响盘/稳定性） |
| 6 | **绘画右栏旧参数面板** | 建议 A 下线（旧 UI 是死控件，实测源不通） | 无 |
| 7 | **v5 安装包细节** | 甲/乙体积、默认盘、签名证书三项——发行启动前最后一批拍板 | 阻塞阶段四 |

---

## 阶段四：主线推进（发行）

### 4.1 发行包实测（拍板 #1/#2 后立即可做，1 天）

- 用现有 make_dist 出一版金母版包（**含本轮新剔除集 ~110M/包**）；
- 🔶 唯一待实测风险点：剔除集在真装机上跑一次**知识库导入**（chromadb 拦截实验是导入级 ✅，但装机级未跑过——这就是为什么要实测）；
- 白名单打包体积应 6.59G → ~6.5G 弱（诚实说：省的是零头，剔除的意义更多是包体干净）。

### 4.2 v5 安装体验（拍板 #7 后启动，估 6~7 周 ⚠️ 沿用既有估算）

Inno Setup + 显卡选套餐 + 模型三通道。本轮回收给它的小礼：模型清单瘦了 12 条幽灵条目、make_dist 过滤器就位。

### 4.3 发行前必修清单（不粉饰）

- **后端三次静默死亡悬案**：根因未闭合（⚠️ 08-29 结论至今）；另 00:59 有一次崩溃检测待查（心跳取证在日志里）——建议发行前做一轮 24 小时挂机稳定性压测；
- P1-5 style rmtree 路径穿越（一行修复+一条单测，半天）；
- 六道锁/AES 金库已就位 ✅，Ed25519 离线激活链路已实测 ✅。

---

## 已知欠账（全量不粉饰清单）

| 项 | 状态 |
|---|---|
| 后端静默死亡根因 | ⚠️ 未闭合（闸门已修，发行前压测） |
| 前端 WS 生图进度恒 0% | ⚠️ 待查（漫剧生图实况） |
| 资产级瞳色/发型一致性协议 | 未做（P2 级） |
| git 索引外来暂存改动 | ⚠️ 待您过目 |
| 绘画持锁时对话秒拒不排队 | 待拍板 #5 一并考虑 |

---

*计划建立：2026-09-02 深夜。变更规则：阶段推进或决策落定后更新对应行；与拍板清单互为引用，冲突以最新实测为准。*
