# 未完成清单（跨模块待办真源）

> 建立于 2026-09-08；2026-09-11 深夜最终重整（详见本文件各节 + UAT 执行记录）。
> **给接手会话的说明**：本清单按「谁阻塞谁」组织。每个条目标注了：
> 能否并行做（不影响其他会话）、需要什么前置条件、做到什么程度算完成。
> 纪律：完成一项就移除并在对应模块文档归档。

---

## 零、立即生效（后端已重启，全部修复已在跑）

以下条目**已经完成并生效**，不需要再做，列出仅供知悉：
- 多角色一致性三版闭环（无锁→软锁→双硬锁，实弹验收通过）
- 模型删除 manifest 同步 + required 护栏（一天三犯根修）
- 知识库批量删除 UI + 图片知识导入（VLM 描述入库+缩略图）
- 知识库搜索 keyword 参数确认正常（此前疑点系排查脚本参数名错误）
- app.css 精确匹配 hex 替换 17 处 + 导入 0 条提示语改进
- 会话日期 1/22 bug 修复（目验通过）
- P1-3 画侧腾挪外提（实弹通过）+ P1-20 账本替换语义 + P1-2 卸载等推理
- 三模块新手引导系统（首跑引导+悬浮按钮，对话/漫画/写作台）
- stop.py 孤儿清理（llama/vLLM 双口径）
- 导入 0 条提示语改进（带可能原因）
- 悬案「幽灵驱动者」已销案（用户裁定人工排查完毕，不再追踪）
- **自愈 Batch 1 报错人话化（2026-09-11 会话B）**：error_handler.py 新增
  SUGGESTION_DEFAULTS 全语义码默认出路表 + error() 自动补逻辑（信封恒带
  suggestion）+ 补登记 12 个漏册码；单测 10/10 + 全量套件 648 绿 + ruff/mypy
  双闸过 + 私有实例 curl 实弹验信封；**未 commit；5800 生产实例下次重启后生效**

---

## 一、可并行开工（无文件冲突，各会话各领各的）

| # | 任务 | 前置条件 | 完成标准 | 方案/真源 |
|---|---|---|---|---|
| 1 | ~~**自愈四批 · Batch 1 报错人话化**~~ ✅ **已完成（2026-09-11 会话B，见下「零」节末条）** | ~~无阻塞，可立即开工~~ | ~~全库 error() 调用带 suggestion 出路提示~~ 已达成 | docs/自愈与横切内建方案-2026-09-10.md §Batch 1 |
| 2 | **知识学习批 1~5** | 无阻塞 | 批0已完成；批1~5按方案逐批推进，每批实弹验收 | docs/知识学习模块升级方案-2026-09-05.md |
| 3 | **升级机制批 2~4** | 批1已入库待过目 | 批2=后端API+前端UpgradeSection；批3=updater本体+boot钩子；批4=双假版本全链实弹 | docs/升级机制方案-2026-09-08.md |
| 4 | **体积优化 A4~A6** | GPU 安静窗口 | A4=V8回退备份11.7G迁移D盘；A5=层5微整洁；A6=层4 junction | docs/体积优化方案-2026-09-10.md §6 |

以下任务需要**同一个文件**（dialog_engine.py），请指定**一个会话**独占处理：
| # | 任务 | 前置条件 | 完成标准 |
|---|---|---|---|
| 5 | ~~**自愈 Batch 2 操作自动记日志**~~ ✅ **已完成（2026-09-11 会话B，df2fbf6）**——实际用中间件 event_log_auto.py 实现，**未占 dialog_engine.py** | ~~独占 dialog_engine.py~~ | ~~用户可感知动作自动 log_event 打点~~ 变更/失败/慢请求自动入时间线，实弹+665 绿 |
| 6 | **自愈 Batch 3 模型未加载自动装载** | 方案=api/deps.py require_engine + 试点绘画生成/漫剧关键帧（均不在 dialog_engine.py，独占约束或可解除） | 未加载时自动加载+进度弹窗+就绪后续跑（已有基础，加固测试覆盖） |

---

## 二、等 GPU 安静窗口（不能与其他 GPU 任务并行）

| # | 任务 | 前置条件 | 完成标准 |
|---|---|---|---|
| 7 | **绘画实弹补验** | GPU 安静（无在途生成+队列空闲） | klein GGUF 出图 1 张 + 漫剧四视图 1 组，产物入库 |
| 8 | **P1-3 画侧腾挪外提实弹** | 同上 | 重构后 load_model 选型/腾挪外提，单角色+多角色格各出 1 张 |
| 9 | **P1-18 FIN 探测调试** | 同上 | 加服务端调试日志定位 watcher 不触发根因；若确认 h11 限制则正式定性关闭 |

**P1-18 已有部分**：断连监视器已接线（1s 轮询 is_disconnected→stop_check 停推理+跳过被动补全+watcher 收尾）；前端 pagehide→keepalive /chat/stop 兜底已实现。
**已知根因方向**：uvicorn h11 对客户端 FIN 半关闭不投递 disconnect 消息。
**实弹结果**：裸 socket 断开后 watcher 未触发（但部分落库/锁释放经异常路径正常）。

---

## 三、需要方案先行（拍板后开工，不可直接写码）

| # | 任务 | 方案状态 | 决策点 |
|---|---|---|---|
| 10 | **自换大模型支持** | 方案成文（自换大模型支持方案-2026-09-10.md），H3=默认主力+可选档已拍 | 是否开工 |
| 11 | **SageAttention V7** | 真轮四坐标已配，带雷（Qwen-Image 黑屏/PuLID 冲突） | 重开提速调查 or 搁置 |
| 12 | **架构总控零散决策** | C5 语音路线试听实测（悬置）；A6 对话排队改进 | 逐项拍 |

---

## 四、挂起（等外部输入/等时机）

1. **云端路线**（漫画模块，「先本地」）：复苏清单见云端API接入方案 §12；
   MiniMax 视频适配器 404 未通。
2. **V10 双卡实机**：等双卡机器。
3. **三插件取包**：zip 通道已探明，待发令。
4. **M1 验收尾巴**：四格第 2/3 格重生示范未做。

---

## 五、待确认 / 待处置（需要用户判断）

1. **5 条「未命名项目」**：幽灵行嫌疑，保留待判断。
2. **GGUF 9B / qwen-edit / wan22 / wan21-vace 回补**：权重已删，恢复=夸克秒传+重新登记。
3. **测试数据清理**：漫画 UAT 项目 4 格三版对比、小说 UAT 8 章 2.7 万字、对话测试会话——保留作证据或清理。

---

## 六、观察项（不改码，盯行为）

- 显存调度：顾问→硬闸翻闸；paint_engine._try_free_vram 收敛。
- MTP 常开（D6-bis）：config 门控待拍。
- 知识库 list query 参数疑未生效（本轮排查脚本参数名错误已销案；但 UI 搜索行为仍建议复测）。

---

## ⚠️ 给接手会话的注意事项

1. **并发纪律**：动 `dialog_engine.py` / `api/dialog.py` / `app.css` 前
   先 `git status` 确认无其他会话在飞（本清单发出时有另一会话的
   worktree 残留已清交卷）。
2. **UAT 自动化配方**：合成 fill 后必须 focus+blur 才触发 onBlur 保存
   （单 fill 不 blur 曾被误判产品 bug）。
3. **检测器类需求**：先在真实语料上验证假设再写码（本轮两条启发式
   全灭教训：功能词密度/句末标点距离均无法区分退化与正常尾部）。
4. **模型删除**：走正规删除端点（DELETE /models/{id}/files），禁止
   raw rm 绕过——manifest 同步已由 a5883f5 根修，直接删目录仍会绕过。
5. **多角色双 PuLID 转正前置**：face_refs↔char_assets 映射校验已通过
   （P0-3 同源同序铁证），双锁实弹 ✅；后续调强度需 GPU 窗口。
