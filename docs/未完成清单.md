# 未完成清单（跨模块待办真源）

> 建立于 2026-09-08；2026-09-10 深夜全面重整（销账：M1 入库、M2/C4 入库、
> 审计 P1×6+P2×10 修复入库、锁语义五连大部分入库、P2-7 裁定并修订 §2.5）。
> 纪律：完成一项就移除并在对应模块文档归档，不在此堆积历史。

## 一、排队受阻（等并发会话 / 等窗口，条件满足即可动手）

1. **对话引擎锁语义三件**（审计上轮 P1-19/P1-23/P1-18）：
   ensure_loaded 双检锁、空闲看门狗补 release_stale、SSE 断连取消生成
   （断连取消语义已拍板，带 3s 刷新宽限）。
   **受阻原因**：并发会话整文件翻写 dialog_engine.py（2052→2138 行）且占
   api/dialog.py 未提交——此刻插手会与其纠缠。**等其提交后约 1 小时可收**。
2. **P2-8 hex 收尾**：语义收编已完成（两批 56 处 + 第二令牌源归一，
   f8fb622）。余量=app.css 61（并发占用，让位后机械+语义各半）+
   LicenseGate 13 / Settings 色板 4 / 品牌渐变停站 ~35 处——已判为
   合法设计字面量（激活门固定配色/预览语义/手工渐变深度），除非
   用户另有要求，不再清零。
3. **P1-3 画侧腾挪外提**：锁序宣言已入库（model_manager 模块头），热路径
   改造须 GPU 实弹窗口逐引擎做（安静窗口 + 可用绘画模型在位）。
4. **绘画实弹补验**（09-10 上午体积清理后未闭环）：安静窗口跑 klein GGUF
   出图 1 张 + 漫剧四视图 1 组。

## 二、待拍板（方案成文 / 决策点明确，等你点头开工）

| 事项 | 现状 | 决策点 |
|---|---|---|
| **模型删除正规入口** | 「删权重不同步登记表」09-10 一天三犯（deepseek/GGUF/qwen-edit+wan×2），全靠提交闸兜底 | 立删除 API（走管理器同步台账+登记表）还是先立人工流程 |
| 自愈与横切内建四批 | 方案成文（自愈与横切内建方案-2026-09-10.md），纯增量估 3-4.5 天 | 批次顺序 / 日志范围 / 试点端点 / 体检白名单四问 |
| 自换大模型支持 | 方案细化完毕（自换大模型支持方案-2026-09-10.md） | 是否开工（H3=默认主力+可选档已拍） |
| SageAttention V7 重开 | 真轮四坐标已配，带雷（Qwen-Image 黑屏/PuLID 冲突） | 重开提速调查 or 继续搁置 |
| 升级机制批2~4 | 批1（打包+验签核心）已完成待过目；D1~D4 已拍 | 过目批1 → 放行批2/3/4 |
| 知识学习批1~5 | 批0 完成，批1~5 待拍（知识学习模块升级方案-2026-09-05.md） | 是否开工 |
| 体积优化 A4/A5/A6 | A1~A3 已执行（144G 隔离区/bf16 分片/deepseek 已删） | V8 回退备份 11.7G 去留 / 层5 微整洁 / 层4 junction |
| 架构总控零散决策 | C5 语音路线试听实测（悬置：不试就维持 SAPI5）；A6 对话排队改进 | 逐项拍 |

## 三、挂起（已拍板，等外部输入 / 等时机）

1. **云端路线**（漫画模块，「先本地」）：底座在库。复苏清单 C1 双路感知 /
   C2 云端接线 / comic.image 槽拍板点见云端API接入方案-2026-09-06.md §12；
   遗留缺陷两枚待拍板：probe 404 假阳性、云端路由时降级横幅误挂+误拉本地
   vLLM；MiniMax 视频适配器形态 404 未通。
2. **V4 云端 Provider / V10 双卡**：V4 随「先本地」延后；V10 等双卡实机
   （多卡地基仅 mock 背书，永久待办直到有双卡机器）。
3. **三插件取包**：StoryDiffusion / ToonCrafter / MangaNinja zip 通道已探明
   （codeload 直下秒通），待用户发令；漫画格变动画若立项优先 Index-anisora。
4. **M1 验收尾巴**：四格漫画第 2 格（丢发卡）/第 3 格（侧脸畸变）重生示范
   未做（单格重生成链已验证可用）。

## 四、待激活 / 待确认（改动已入库，产品尚未吃到）

1. **后端重启激活**：今日入库的 P2 批次（同步 IO 卸载/路径约束/CSV 中和）、
   锁语义 P1-20/P1-2、批2 骑乘等，须后端重启生效（运行中实例是 11:27
   拉起的，只含上午的小扫除）。前端 dist 已构建，浏览器刷新即见。
2. **5 条「未命名项目」**（projects 表，各带 1~6 分镜行，11:23~11:24 产生，
   幽灵行嫌疑不能定论）：按「来历不明不动」保留，请你判断后删除或认领。
3. **GGUF 9B / qwen-edit / wan22 / wan21-vace 回补**：权重已删、登记条目
   已同步（22→18）。要恢复 = 夸克网盘秒传 + 登记表重新登记（代码链完好）。

## 五、观察项（不改码，盯行为）

- 显存调度：顾问→硬闸翻闸（重启后 e2e_gpu_budget_live.py 观察）；
  paint_engine._try_free_vram 收敛；开机 9B 预载与手动装载互踩窗口
  （9B GGUF 已删后此条自动缓解，待复核）。
- MTP 常开（D6-bis）：+39% 已实证，16G 装不下常开，config 门控待拍。
