# 未完成清单（跨模块待办真源）

> 建立于 2026-09-08。收录已拍板挂起/等待窗口/等待外部输入的事项。
> 完成一项就移除并在对应模块文档归档，不在此堆积历史。

## 云端路线（漫画模块，2026-09-08 用户裁定挂起：先本地）

底座已建好（云端API接入批1~3：五工位绑定/三套适配器/双队列云端道/协议感知探测），以下为复苏时的工作清单：

1. **C1 双路感知**：漫画工作台「出图引擎」状态胶囊（本地显卡 ⇄ 云端·服务商名）+ 点击直达 keyframe.image/asset.image 工位绑定/解绑面板；生成按钮/浮层显示实际路由（后端响应 `cloud` 字段已就绪：keyframe.py:2500/2565、comic_asset.py:140/273）。
2. **C2 云端接线**：AI 写分格的 manga.text 云端槽优先路由（本地路先行已建；补 5 行槽位解析即接，参考 storyboard.py `_ai_split_block_sync` 模式）。
3. **P1 拍板点**：keyframe.image/asset.image 工位漫剧+漫画共用 vs 新增 `comic.image` 独立槽（云端复苏时定）。
4. **前置缺口（继承自云端API接入方案-2026-09-06.md §12）**：云端全部实弹是 mock 无真实 Key、前端未真浏览器实测——需真实服务商 Key 做真云端验收。

## 三插件取包（M3 备选，2026-09-08 网络诊断后通道已探明）

- StoryDiffusion / ToonCrafter / MangaNinja(ComfyUI 版)：git 协议被重置，但 **codeload.github.com zip 直下实测秒通（HTTP 200，0.5s）**——换「下 zip 解压」即可取包，待用户发令。
- 新候选：bilibili/Index-anisora（2.5k⭐，2026-07 活跃，权重在 ModelScope 国内直下）——若做「漫画格变动画」优先于 ToonCrafter。

## 其他跨模块遗留

- M1 漫画页改动未 commit（连同小说迭代/绘画清除/M0），等用户拍板提交。
- 漫画 M2（本地部分）：对话框/台词文字层 + 整页导出 PNG/PDF + 阅读视图（=升级方案 C4，对齐 AIMangaStudio 导出概念）。
- M1 验收遗留：四格中第 2 格（奔跑丢发卡）/第 3 格（侧脸畸变）建议重生示范未做（单格重生成链已验证可用）。
- 实机双卡验证缺口（多卡地基仅 mock 背书，永久待办直到有双卡机器）。
