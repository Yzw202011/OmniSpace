# 显存调度机制·严格测试报告（第一轮，2026-09-10 03:38–04:25）

> 执行：完整版 D-t1=A（S5 浏览器层与部分长项待续，见 §五）。被测 HEAD=0f6a252 + 测试期修复（F-1/2/3，已过闸未 commit）。
> 后端两次正规链重启（03:39 / 03:47，后者载入修复）。GPU 活动门每轮前置核查通过。
> 证据均摘自 logs/backend.log 原文行、API JSON、nvidia-smi 采样、进程表。

## 一、结果总览

| 层 | P0 过/总 | P1 | P2 | 备注 |
|----|---------|----|----|------|
| S0 静态 | 5/5 ✅ | — | — | 585 unit+69 vitest+tsc+ruff+mypy |
| S1 健康 | 5/6 ✅ | 2/2 ✅ | — | S1.5 真浏览器 9 页待续 |
| S2 功能 | 11/15 ✅ | 3/5 ✅🔶 | — | S2.8 判 ❌→发现 F-4；3 项待续 |
| S3 冲突 | 6/10 ✅ | 2/8 | — | 4 项 P0/6 项 P1 待续（单窗口时间限制） |
| S4 soak | 在飞 | — | — | 03:55 起 30min，04:25 四查 |
| S5 前端 | 1/6 ✅ | — | — | build ✅；浏览器层整层待续 |
| S6 基准 | 4/5 ✅ | — | — | S6.3 装载时长=9B 本机不可达改判 |
| S7 回归 | 3/5 ✅ | — | — | S7.3 关键帧/S7.4 H3 待续 |

**P0 判定：45 项已测 35 过 1 败（F-4）9 待续**；测试期发现并修复 3 个缺陷（闭环验证通过）。

## 二、关键证据摘录

- **S2.12 孤儿收账（活体）**：`03:39:15 清扫 vLLM 孤儿进程 pid=24128`——上会话遗留的 vLLM（占 11GB）新代码启动即收。
- **S3.6 GGUF×绘画驱逐链（三证齐全）**：`驱逐最低优先级模型: qwen35-9b-gguf-q4km (priority=5, keep=['image','paint','vision'])` → `llama-server 已停止` → `klein 装载成功(12.8s)` → 任务出图 → 下次对话自动重载 GGUF（首字 517ms 含重载）——驱逐统一语言+UX 铁律全链真机工作。
- **S2.7/S2.2 去抖精确 10s**：`03:44:07 排空` → `03:44:17 唤醒（批3 去抖单源，source=image_queue）`。
- **S2.9 取消不泄漏**：排队取消（was_pending=true）与运行取消（cancelled）后 `/models/vram` busy_registry 均清空。
- **S2.6+S7.5 云链**：mock provider 注册→绑定→云图 4.5s 完成（`云端图片完成: mock-cloud 512x512`）→上榜条目 `{paint, cloud}` 取证→完成下榜干净→解绑恢复本地。
- **S3.8 双服务对撞**：GGUF 在跑时 load 9B → 准入诚实拒绝（13.3<14.9 带出路文案）、GGUF 未被误杀、显存无叠爆。
- **S6 基准**：GGUF 105 tok/s（POC 114，≥90 ✅）/装载 7s/首字中位 461ms（3 轮 358/517/461）；klein 峰值 13.8GB（V8 基线 14.1，−2% ✅）。
- **S1.6 互斥体**：二次启动 rc=3 `单实例限制：本机已有后端实例在运行`。
- **顾问帧成对**：每任务 granted+release 各 1 帧（五连发 9 对计数吻合）；release 帧带 feature 名（F-2 修复后）。

## 三、发现项（按严重性）

| # | 发现 | 状态 |
|---|------|------|
| **F-4** | **torch/NVML 双口径差 6.18GB**（GGUF 子进程在跑时 API free=13.37 vs NVML 7.19）——gpu_budget 乐观口径会高估空闲，**12GB 级需求会误放行致 OOM**；**硬闸翻闸的前置阻塞项**（修复方向：read_physical_bytes 取 min(torch, nvml) 保守口径） | ❌ 未修（顾问模式无实害，硬闸前必修） |
| F-3 | llama-server 失联无自愈（杀进程后对话持续报错） | ✅ 已修复+闭环验证（自动重载续跑，18.5s 含重载） |
| F-1 | 自动选档可见性漏动态发现兜底分支（autoselect 事件 0 条） | ✅ 已修复+验证（事件出现，两轮预热各 1 条） |
| F-2 | release 帧 feature 显示 "?" | ✅ 已修复+验证（release paint） |
| F-5 | comfy_proc 无孤儿收账（启动期 ComfyUI 遗留进程需手清——本轮实测遇到，靠 Job Object 失效场景） | 未修（建议补收账） |
| F-6 | Git Bash 中文 JSON 转义坑（测试工具问题，非产品） | 记录 |

**修复三件（dialog_engine×2、gpu_budget×1、两队列×1）已过 ruff+定向单测 48 绿，未 commit（待拍板）。**

## 四、诚实判定与改判

- S2.5（净窗口 9B 恢复）**改判 🔶**：本机白天空闲 13.3GB < 9B 需 14.9GB——**9B 常态不可达是设计已知的硬件事实**（方案 §1.3），"恢复态"=GGUF 档（已验证自动重载）。
- S2.2（video 去抖）**🔶**：去抖协调器代码路径已被 image 侧 10s 精确验证（同一函数），video 真任务未跑。
- S2.8 **❌**：三方对账超差——即 F-4（口径差量化确认，非新引入回归）。

## 五、待续清单（建议下一窗口续测）

S1.5/S5 全层（真浏览器 9 页+气泡语义+WS 帧+设置面板）；S2.13/14（临时阈值空闲链）；S2.2 video 真任务；S3.1 全组合/S3.9/S3.10/S3.13-18；S7.3 关键帧/S7.4 H3/S7.6-7.10；S6.4/6.8（soak 采集）；S2.17/S4.7-4.8 观察项。

## 六、soak 终局与第二轮补充（04:25–06:45）

- **soak 30min/60 轮完成**：无死锁（对话 50+ 轮成功）、显存无单调泄漏（基线 9528 → 末轮 9012MiB，方向为降）、后四查全零（busy 空/预留 0/台账 0/显存回 1.99GB 基线）——**S4.1/4.2/4.3 ✅**。
- soak 期 60 条绘画失败=**测试环境残留非产品缺陷**（解绑 API 误用 PUT{} 实为失败未生效 + mock server 随脚本退出）——根因查明并复原（删 provider 级联清绑定，复原后本地绘画 7.8s 恢复 ✅）。记 **F-7 观察项**：云绑定残留且云不可达时绘画持续失败不回落本地——失败提示是否带"检查云端连接/解绑"出路值得复核。
- **S2.14 看门狗自然触发 ✅**：soak 结束后 900s 空闲 → 对话模型走正规链卸载 → 显存回干净基线（无需临时改阈值，真实时长验证）。
- **浏览器层（第二轮）**：9 页 DOM 渲染全过（S1.5/S5.6 ✅，节点 227-3732 无白屏）；模型页 GGUF 条目+装卸按钮可见（S5.5 ✅）；设置页四新面板全渲染（S5.4 ✅：云端/联网搜索/界面打开方式/性能模式）；WarmupBar 装载提示+消息排队文案真实出现（S5.7 ✅）；气泡交互 🔶（IAB 输入时序受限，模型名正确性已有 API 层 meta.model 多轮证据）。
- 净窗口 9B：测试尾段引擎在空闲 ~14GB 下重新预热装载（候选链首位 9B est 13.5 ≤ 14 命中）——装载已在进行，完整流式验证交由正常使用观察（🔶）。

## 七、环境复原确认单（测试结束态）

| 项 | 状态 |
|----|------|
| config.yaml | 零改动 ✅（diff 对快照） |
| 云 provider/绑定 | mock 已删、绑定清零 ✅ |
| soak 脚本/日志 | 已清理 ✅ |
| 临时阈值 | 未曾修改（看门狗自然触发验证，无需改回）✅ |
| 工作区剩余 | F-1/2/3 修复 4 文件（过闸未 commit，待拍板）+ 测试方案/报告 2 文档；comfy_model_map.json 与 uno_smoke.py 为并发会话产物（未动） |
| 后端 | 保持运行（新代码+修复已生效） |

## 八、最终矩阵（两轮合计）

**P0 已测 42 项：39 ✅ / 1 ❌（F-4 口径差，硬闸前置阻塞）/ 2 🔶（S2.5 净窗口装载观察中、S5.2 气泡交互）**。发现项 7 个（F-1/2/3 修复闭环、F-4 待修、F-5/F-6/F-7 记录）。待续清单维持 §五（关键帧/H3 真任务、S3 剩余组合、WS 帧取证、视觉回归等）。
