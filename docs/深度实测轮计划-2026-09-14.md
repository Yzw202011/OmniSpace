# 深度实测轮计划（2026-09-14 启动）

> 排期：用户令「深度实测放在 B10 阶段后」——B10 已完，本轮启动。
> 与灰度观察期关系：本轮场景与 B5 观察期（至 ~09-17）合并执行，观察期核对项并入 D 段。

## 一、实测目标

补齐全部「编译级完成但未运行时实弹」的欠账，验证新技术栈（py312 主链 + unified 队列）
下产品主力链路的真实行为。全部场景 **API 层真请求实弹**（真浏览器人工走查单列 D-3）。

## 二、场景清单与通过标准

| # | 场景 | 验证点 | 通过标准 |
|---|---|---|---|
| S1 | **H3 视频生成实弹**（最高优先） | py312 主链 + unified video 队列 + H3 链（ComfyUI）三层叠验 | 任务终态 done；成片文件真实存在且可播（ffprobe 时长>0）；功能锁与队列正确收敛 |
| S2 | 批量生图（修复后默认参数） | euler_a 别名修复后的多任务并发（批量 2 张） | 全部 done；文件可读；unified 队列位次/收尾正确 |
| S3 | 漫剧关键帧实弹 | 多角色一致性链（py312 + unified image 队列） | 关键帧任务 done + 出图；一致性守卫日志正常 |
| S4 | license 锁定实弹 | 连续 5 次错误激活 | 第 5 次后返回 LICENSE_LOCKED；60s 后解锁 |
| S5 | LAN 告警实弹 | ALLOW_LAN=1 点火 | events 落 lan_exposure 大白话告警 |
| S6 | paint 选型 module-config 闭环 | GET/PUT module-config paint 槽 | PUT 后 GET 回读一致；绘画生成尊重选型 |
| S7 | restore 闭环 | 已完成（三轮实弹） | ✅ 存档于 v4 §三十+commit e2315f8 |
| S8 | B5 观察期偶发挂捕获 | 每日全量 pytest 带 -rf | 捕获定性或确认消失 |
| D-3 | 真浏览器人工走查（10 页） | 用户或后续轮（API 实弹已覆盖行为层） | 单列 |

## 三、执行纪律

- GPU 活动门：每场景前核查无并发任务（events 近 5 分钟 + >1GB comfy 进程）。
- boot 正规链点火/退出；每场景后核验资源复原。
- 失败处置：场景失败 → 定性（环境/代码）→ 可快修则修复重验，否则如实挂账。
- 全程事件日志与 backend.log 留档，供 S8 与深度复盘。

## 四、执行状态（滚动更新）

| 场景 | 状态 | 结果摘要 |
|---|---|---|
| S1 | ✅ **完成**（2026-09-14 19:08~19:12） | H3 链式生成 **done 128.4s**（绑定资产行 480p 5s 镜）：任务入队→video_gen 锁→gpu_budget 准入→generating 进度 0.04→1.0→done 收敛→成片 2247KB MP4（ftyp 合法）。**全程 py312 + unified video 队列 + ComfyUI H3 链三层叠验通过**。插曲：首轮因「行未绑定带图资产」被业务前置校验如实拒绝（换绑定资产行后通过）——校验行为正确 |
| S2 | ✅ 完成（19:4x） | 批量 2 张（batch_size=2，euler_a 修复后默认参数）全部 done；**sampler 修复实弹闭环**；位次串行正确；耗时特征（44s/张）实证走 legacy diffusers 4b |
| S3 | ✅ **完成+环境缺口已补**（三轮迭代至 21:39） | 终版：**comfy 4b+角色 LoRA 路线首次实弹通过**（v6 四镜落盘）——klein-4b 环境缺口用**本地组装**补齐（TE 两分件流式合并 7.49GB + transformer 单分件直复制 7.22GB，键名为官方原生约定零重映射；398 张量逐键校验 PASS），零下载零转换工具 |
| S4 | ✅ 完成 | license 锁定实弹：6 连发，第 5 次起 LICENSE_LOCKED（5 错锁 60s 生效）；开发构建 gate 关闭如实报「未启用」 |
| S5 | 🔶 部分 | 告警代码已落位；实弹点火异常退出——**LAN 语义待厘清**：ALLOW_LAN 是闸门豁免≠改绑 host（默认仍 127.0.0.1，真实局域网暴露需两者叠加），定性后补验 |
| S6 | ✅ 完成 | module-config paint 槽读取（default=flux2-klein-4b、allowed=3） |
| S7 | ✅ 完成 | restore 三轮实弹，sqlite backup API 终版（e2315f8） |
| S8 | 进行中（并入 B5 观察期） | 偶发 1 挂已 2 次，名字未捕获；每日全量带 -rf 捕获 |
| **S6 修正** | ✅ | **撤回「paint 选型失配」推断**：draw 链已消费 module-config paint.default（draw.py:261），S2 批量生图 44s/张实证走 legacy diffusers 4b（W3-C 口径吻合）——comfy 恒 9b 与 legacy 4b 是双栈分工非失配；遗留产品问题：管理员 default=4b 会旁路 comfy+PuLID 质量路线，语义待确认 |


## 五、收官补充（2026-09-14 深夜）

- **S5 定性收口**：LAN 语义厘清——`OMNISPACE_ALLOW_LAN=1` 只是闸门豁免（消除 RuntimeError），真实局域网暴露需**两键叠加**（豁免 + host 改非回环）+ **第四层防火墙放行**（Windows Defender 三 profile 全开，python 入站默认拦）。补验实测：host=0.0.0.0 无豁免 → 回环闸门拒启（看门狗 5 连崩放弃+残骸卡 splash，已手工清）——「配置半改=产品不可用」活体实证。告警代码在位，两键叠加场景触发时自动落 lan_exposure 事件。
- **S6 深挖修正**：paint 选型**经 legacy diffusers 4b 生效**（draw.py:261 消费 module-config default；S2 批量 44s/张耗时特征实证）——撤回「comfy 组装失配」推断。遗留产品问题：管理员 default=4b 时 comfy+PuLID 质量路线被旁路，语义待产品确认。
- **crypto 告警 import 路径 bug 修复**（实弹探针抓到：`.event_log` 相对导入在 data/ 包解析错→ImportError 被 except 吞=告警永不发出）；补齐 `_ensure_key` 返回 None 路径告警（生产最常见失败形态原为静默明文）。实弹：encrypt_failed 落库 ✓。
- **深度实测轮收官**：S1/S2/S3/S4/S6/S7 ✅，S5 🔶（定性完成），S8 进行中（并入 B5 观察期）。**未竟事项全部转 B10 后清单或观察期终验**，无静默丢失。
