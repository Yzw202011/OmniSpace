# 深度实测轮计划（2026-09-14 启动）

> 排期：用户令「深度实测放在 B10 阶段后」——B10 已完，本轮启动。
> 与灰度观察期关系：本轮场景与 B5 观察期（至 ~09-17）合并执行，观察期核对项并入 D 段。

## 一、实测目标

补齐全部「编译级完成但未运行时实弹」的欠账，验证新技术栈（py312 主链 + unified 队列）
下产品主力链路的真实行为。全部场景 **API 层真请求实弹**（真浏览器人工走查单列 D-3）。

## 二、场景清单与通过标准

| # | 场景 | 验证点 | 通过标准 |
|---|---|---|---|
| S1 | **H3 视频生成实弹**（最高优先） | py312 主链 + unified video 队列 + H3 链（ComfyUI）三层叠验 | 任务终态 done；成片文件真实存在且可播（ffprobe 时长>0）；功能锁与队列正确收敛 |
| S2 | 批量生图（修复后默认参数） | euler_a 别名修复后的多任务并发（批量 2 张） | 全部 done；文件可读；unified 队列位次/收尾正确 |
| S3 | 漫剧关键帧实弹 | 多角色一致性链（py312 + unified image 队列） | 关键帧任务 done + 出图；一致性守卫日志正常 |
| S4 | license 锁定实弹 | 连续 5 次错误激活 | 第 5 次后返回 LICENSE_LOCKED；60s 后解锁 |
| S5 | LAN 告警实弹 | ALLOW_LAN=1 点火 | events 落 lan_exposure 大白话告警 |
| S6 | paint 选型 module-config 闭环 | GET/PUT module-config paint 槽 | PUT 后 GET 回读一致；绘画生成尊重选型 |
| S7 | restore 闭环 | 已完成（三轮实弹） | ✅ 存档于 v4 §三十+commit e2315f8 |
| S8 | B5 观察期偶发挂捕获 | 每日全量 pytest 带 -rf | 捕获定性或确认消失 |
| D-3 | 真浏览器人工走查（10 页） | 用户或后续轮（API 实弹已覆盖行为层） | 单列 |

## 三、执行纪律

- GPU 活动门：每场景前核查无并发任务（events 近 5 分钟 + >1GB comfy 进程）。
- boot 正规链点火/退出；每场景后核验资源复原。
- 失败处置：场景失败 → 定性（环境/代码）→ 可快修则修复重验，否则如实挂账。
- 全程事件日志与 backend.log 留档，供 S8 与深度复盘。

## 四、执行状态（滚动更新）

| 场景 | 状态 | 结果摘要 |
|---|---|---|
| S1 | ✅ **完成**（2026-09-14 19:08~19:12） | H3 链式生成 **done 128.4s**（绑定资产行 480p 5s 镜）：任务入队→video_gen 锁→gpu_budget 准入→generating 进度 0.04→1.0→done 收敛→成片 2247KB MP4（ftyp 合法）。**全程 py312 + unified video 队列 + ComfyUI H3 链三层叠验通过**。插曲：首轮因「行未绑定带图资产」被业务前置校验如实拒绝（换绑定资产行后通过）——校验行为正确 |
| S2~S6 | 待执行 | — |
| S7 | ✅ 已完成 | 三轮实弹，sqlite backup API 终版（commit e2315f8） |
| S8 | 进行中（并入观察期） | 已 2 次，名字未捕获 |
