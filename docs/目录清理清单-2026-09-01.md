# 项目目录存量清理清单（已执行完毕）

> 生成：2026-09-01，由 `scripts/dir_audit.py` 首轮体检产出（规范见 `docs/项目目录规范.md`）
> **执行状态：用户拍板「全按建议执行」，2026-09-01 当日执行完毕，审计复跑归零（根目录 10/10、顶层 29/29、模型副本 7 对全部硬链接合规）。**

## 执行结果记录

| 项 | 结果 |
|---|---|
| A 直接删 | 76 项删除（含 tmp_key.txt 敏感文件，git 历史核实从未提交过）；e2e_manga_test.py 有打包链留档价值 → `scripts/scratch/` |
| B 资产归档 | **7 个** A~E 编号历史文档（含漏网的编程语言规范A.txt）→ `docs/历史资料/`；17 张截图 → `docs/assets/` |
| C exe 处置 | `Omnispace.exe` 已删；`启动OmniSpace.exe` 为源 exe 保留并已入白名单 |
| D 目录迁移 | modelxiazai → `scripts/comfy_link/`（make_dist.py 已改：包内仍落 modelxiazai/，交付物路径不变）；cssc/temp_uat×3/_p3_smoke×2 → `scripts/scratch/archive/`；performance-optimization-plan → `docs/历史资料/`；omnipspace-demo 零引用已删；monitor.py 探针注释路径同步更新 |
| E VAE 合并 | 双 VAE 保 ComfyUI 侧（在用且新），项目侧换硬链接，**释放 5.4GB**（E 盘可用 98.6G→104G），审计模型副本 7/7 同 inode |

---

以下为原始清单（留档对照）：

---

## A. 建议直接删除（低风险垃圾，共约 95 项）

| 类别 | 明细 | 理由 |
|---|---|---|
| **敏感文件** | `tmp_key.txt` | 密钥类明文，删后应检查是否曾入 git（已确认 .gitignore 拦截，但需复核历史提交） |
| 临时输出 ×5 | `tmp_fails.txt` `tmp_learn_run.txt` `tmp_survey_out.txt` `tmp_worst_out.txt` `_v25_resp.json` | 调试输出残留 |
| 旧日志 ×2 | `debug.log` `pyarmor.bug.log` | 日志归宿是 logs/，根目录不留 |
| 运行态残留 | `e2e_state.json` | e2e 框架状态文件 |
| 调试脚本 ×~85 | `_tmp_*.py` `tmp_*.py` `_sandbox_*.py` `_mcfg.py` `e2e_manga_test.py` | 历史探针/冒烟，全部为一次性用途；.gitignore 本就不追踪。若个别有留档价值 → `scripts/scratch/` |

## B. 建议移入 docs/（是资产不是垃圾，共 23 项）

| 类别 | 明细 | 去向 |
|---|---|---|
| **编号文档 ×6** | `程序开发计划文档B.txt` `全量项目技术文档C.txt` `极致细粒度全量文档E.txt` `布局视觉与规范D.txt` `标准裁定书.txt` `开发清单.txt` | 自带「仓库外源历史文档」声明（958~3591 行），**→ `docs/历史资料/`** |
| 截图 ×17 | `*.png.jpg` `2026-08-25 190520.png` | 页面验收截图 **→ `docs/assets/`**（git 白名单已配）或拍板删除 |

## C. 查明用途后处置（2 项）

| 明细 | 现状 | 建议 |
|---|---|---|
| `启动OmniSpace.exe` | 44KB 启动器**源 exe**（09-01 裁定 bat→启动程序时定下"仓库根留源 exe"，已入白名单） | **保留**（勿删） |
| `Omnispace.exe` | 与上者**字节完全相同**（MD5 一致）的改名复制品 | 删除（冗余副本） |

## D. 顶层目录 9 个白名单外

| 目录 | 内容 | 建议 |
|---|---|---|
| `modelxiazai/` | **有价值的挂接工具**：`link_comfy_models.py` + `comfy_model_map.json` | 迁 `scripts/comfy_link/` 后删原目录（规范 §2.3 引用同步改） |
| `cssc/` `temp_uat/` `temp_uat_fine/` `temp_uat_manga/` `_p3_smoke/` `_p3_smoke3/` | 历史探针/UAT/打包冒烟脚本 | 归档 `scripts/scratch/` 或拍板全删 |
| `omnipspace-demo/` | 早期 demo（拼写错误目录名） | 确认零引用后删 |
| `performance-optimization-plan/` | 历史 HTML 优化报告 | 迁 `docs/历史资料/` 或删 |

## E. 模型重复副本（释放约 5.4GB，需拍板保留哪版）

| 文件 | 两边现状 | 建议处置 |
|---|---|---|
| `minimax_h3_video_vae_fp16.safetensors` | ComfyUI 侧 4.85GB（**08-29，实际在用**）vs 项目侧 4.85GB（08-25），**字节差 61，非同源** | 保 ComfyUI 侧（在用且更新），项目侧替换为硬链接 → 释放 4.85GB |
| `minimax_h3_audio_vae_fp32.safetensors` | ComfyUI 侧 0.56GB（08-29）vs 项目侧 0.56GB（08-25），字节不同（低于审计 1GB 门槛未入报告） | 同上 → 释放 0.56GB |

> 更正说明：此前口头说「VAE 可零风险硬链接合并」不准确——实测两边是**不同修订版**（字节数不同），合并 = 拍板丢弃项目侧旧版。因项目侧无任何加载方（h3 引擎消费的是 ComfyUI 侧），风险实际趋零，但按规矩报你裁定。

---

## 预期收益

- 磁盘释放：约 5.4GB（E 项）+ 零头（A/D 项多为小文件）
- 根目录从 111 个文件 → 9 个白名单文件
- 顶层目录 38 → 29（全部在白名单）
- 审计脚本退出码归零
