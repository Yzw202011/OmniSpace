# 绘画方向拍板材料包（2026-09-17，供用户挑选）

> 背景：09-15 凌晨绘画五轮 A/B（樱花/海滩/3D GC/纯文字/四视图对标竞品）留下四个
> 待拍板方向。本材料包按**当前现状**重新对账后成文——其中一项已被后续会话做完。
> 数据真源：深度实测轮计划-2026-09-14.md（收官节）+ 09-15 A/B 实测记忆 + 本次实测。

---

## 先销案：四视图对标竞品 ✅ 已做完

**7fb0957 已实装 zviews 分张管线**（Z2/ZC）：
- 逐视图生成 + 参考锚身份 + **代码贴字**（中文标注零乱码——对标竞品的头号硬伤根除）；
- `src/config.yaml:174 turnaround_engine: zviews` 默认开启；云端分支 ZC 也在。
- 09-16 审计复核：zviews 链路完整默认开（comic_gen.py:937-1144）。
- **剩余**：无代码项。若你想眼见为实，可在 GPU 空闲窗口跑一组四视图实拍（约 2 分钟/视图）。

---

## 待拍板一：LoRA 脸部身份接产线

**现状**：09-15 A/B 定论「脸部还原 legacy 软锁 > LoRA > PuLID」（真人参考脸）。
attach_lora 已有生产调用（keyframe.py:1555，旧「零调用」系误报）；D-LoRA 角色回落
快线的 lora.safetensors 约定挂载链完整（keyframe.py:964-970）。

**选项 A：接产线（推荐）**——把「角色绑定 LoRA → 生图自动挂载」设为默认行为。
- 成本：约半天（接线 + 冒烟）；LoRA 文件本身需按角色已训（现有角色若未训则回落参考图锚，行为不变）。
- 收益：有 LoRA 的角色脸部稳定性直接提升一档（A/B 实测排序）。
- 风险：低——无 LoRA 时回落现状，零破坏。

**选项 B：保持手动**——现状不动，用户自己保证 LoRA 在位。
- 成本 0；代价=多数用户永远用不上这条快线。

## 待拍板二：纯真人重训 LoRA

**现状**：PuLID 对真人参考脸型漂移（动漫角色才是强项）；legacy 软锁要 9B 在位。

**做什么**：用真实人像素材集重训一个「真人底 LoRA」，让真人参考不再依赖 9B 软锁。
- 成本：半天~一天 GPU 窗口 + 素材（每人 10~30 张多角度清晰照）。
- 收益：真人参考脸型漂移根除；16G 卡常驻可用（不再要 9B）。
- 风险：中——素材质量决定成败；训练占显存窗口（期间绘画不可用）。

## 待拍板三：qwen-image-2512 换牌

**现状**：换牌需**空闲 RAM ≥22G**。实测今天空闲 17.2G（比 09-15 的 18.7G 更紧）。
- **建议：挂起**。除非重启清缓存+关闭大应用腾到 22G+，否则装不下。
- 若未来可行：四视图写实度天花板有望抬一档（对标竞品硬伤之三）。

---

## 附带悬案（S6 遗留）：管理员 paint.default=4b 语义

深度实测 S6 撤回「选型失配」推断后遗留：管理员默认 4b 时，**comfy+PuLID+zviews
质量路线被旁路**（legacy diffusers 4b 直出，44s/张）。

- **A. 默认改走 comfy 质量路线**——质量优先（PuLID 一致性+zviews 全开），首图慢（comfy 冷启动）。
- **B. 保持默认 4b**——速度优先（44s/张暖态），要质量时用户手动切。
- **C. 首次使用引导**——首次进绘画页弹一次选择（质量/速度），选完记住。

---

## 汇总速答表（你只需回四个字母）

| # | 方向 | 推荐 |
|---|------|------|
| 1 | LoRA 接产线 | **A 接**（半天，低风险高收益） |
| 2 | 纯真人重训 LoRA | 有素材再训（先备素材） |
| 3 | qwen-image 换牌 | 挂起（RAM 17.2G < 22G） |
| 4 | paint.default 语义 | C 引导（或 A 质量优先） |
