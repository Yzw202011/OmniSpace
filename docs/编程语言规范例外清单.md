# 编程语言规范例外清单（2026-09-04 起）

> 规范真源：`docs/历史资料/编程语言规范A.txt` 适用部分；计划：`docs/编程语言规范合规计划-2026-09-04.md`。
> 例外 = 规范字面与工程现实的**已批偏离**，新会话不得照抄例外写新代码，也不得擅自「修复」例外项。

## 范围冻结（用户令 2026-09-04）

`packaging_console/`（打包台）与 `license_console/`（发码台/激活台）**代码不动**（其自身 bug 修复除外），
合规改造、类型标注统计、闸门验收均不含此二台。用户原话：规范后项目功能都能正常使用，除打包台和激活台。

## 例外列表

| 编号 | 例外 | 现状证据 | 不改的理由 | 批准 |
|---|---|---|---|---|
| E1 | SQLite 用同步 sqlite3，未用规范指定的 aiosqlite | 8 文件 `import sqlite3`；WAL+`_write_lock`（database.py:317,707） | 319 端点数据层全量迁移回归风险 >> 收益；单实例互斥已杜绝 F-015 | D2 待批 |
| E2 | AI token 流走 WS（/dialog/stream），规范 F-011 指定 SSE | ws.ts 三端点之一 | 核心功能存量迁移动静大；**新代码禁新增 WS 传 token 流** | D2 待批 |
| E3 | 发码台内置明文管理员密码 | license_console/app.py:52（该台已冻结） | 用户既定设计 | 用户已拍板 |
| E4 | ErrorBoundary 用 class 组件 | App.tsx:123 | React 官方唯一必须 class 的场景（错误边界），业界公认例外 | D2 待批 |
| E5 | scripts/test_latency_fix.ps1 共 54 行（F-012 上限 50） | scripts/test_latency_fix.ps1 | 测试用途超 4 行，改写收益为零 | D2 待批 |
| E6 | 布局类 width 过渡保留：`.sidebar`（app.css:252，260↔64px 收展）+ `.manga-video-prog-bar` 类级 width 过渡（app.css:1293，视频块多用法共用） | 批 1 落地时逐处评估 | transform 化需重构布局流/统一迁移多处用法，风险 > 收益；低频操作非动画热路径。**进度条类已全部 scaleX/scaleY 化（批 1），此条仅剩布局类** | 批 1 产出 |
| E7 | qwen_gguf_stream.py 4 个 `@triton.jit` 内核（_deq_q4k/_deq_q5k/_deq_q6k/_deq_q80，L64/85/108/125）不加 Python 类型注解 | 批 2 收官唯一剩余（全仓 2356/2360=99.8%） | triton JIT 会解析内核签名注解驱动其编译特化（constexpr 识别），加 Python 注解会进入 GPU 编译管线、可能改变 GGUF 反量化行为——正是「功能零影响」铁律的适用场景 | 批 2 产出 |
| E8 | sakura.css 性能模式块 5 处 `!important`（animation-duration/iteration/transition-duration/backdrop-filter×2） | frontend/src/styles/sakura.css:1110-1114（html.ui-lite 全局降载块） | 「禁用级覆盖」唯一合法用法：须压过组件内联 animation 与第三方库样式实现一键停动画/去毛玻璃；仅此块允许，其余位置禁用 !important 铁律不变（问题总账 #19） | 2026-09-19 Q2 批 |
