# OmniSpace 问题总账（2026-09-18 · 六轮深查合并 · 零文档依赖）

> **定位**：对账系列第六册=**问题总汇总**。六轮只读深查（功能基线→活体深查→根查断链→对账总表→散乱度→本轮纪律/结构深挖）的全部发现合并去重，按严重度排列，每条带证据位与轮次来源。分册证据链见同目录前五册。
> **方法**：全部出自源码解析/只读 DB/磁盘实测/活体探针/机械交叉核；无一条引自 docs 宣称。
> **快照**：2026-09-18 深夜 · HEAD=`f610226`。

---

## 一、问题清单（42 项：🔴2+1红带 / 🟡22 / 🟢17+6；第十轮增补 37-42 为屎山指标与文档债）

### 🔴 功能与契约级（2）

| # | 问题 | 证据 | 来源轮 |
|---|---|---|---|
| 1 | **f5_tts 依赖未钉版、主运行时不可导入**——音色克隆功能码在库但当前机器休眠；按 lock 出包必缺件 | voice_clone_engine.py:31,45 懒加载；requirements-lock 无此包；py312 实测 import 失败 | 总表轮 |
| 2 | **接口别名前缀族**——draw.py 一文件三族（/draw×13+/paint×10+/art×1）；dialog.py 两族且倒挂（/chat×16+/dialog×6，主体与模块名不符）；消费端各认各前缀（/draw/upscale 与 /paint/upscale 都有人调） | 装饰器逐一直读+消费交叉 | 散乱轮 |

### 🟡 标准与工程纪律级（11）

| # | 问题 | 证据 | 来源轮 |
|---|---|---|---|
| 3 | **93 处端点收裸 dict body**（对抗「API 数据模型 MUST Pydantic」自家铁律；粗口径含少量内部助手） | api 层正则计数 | 本轮 |
| 4 | **错误码双轨**：语义串为标准，但 ~53 处仍直写裸数字（60003 型，comfy 链最多） | 全库正则（量级口径） | 散乱轮 |
| 5 | **DF 内核子树游离于本家纪律外**：187 处 print（不在 logging 体系）+37 条 mypy 基线豁免——全部位于 src/{training,core,codec,cutemamen,data/real_dataset}（PR#3 并入的外来层理，原生子树 api/services 为 0） | print 分布定位：train.py 41/trainer.py 33/distributedformer.py 24/… | 本轮 |
| 6 | **logger 变量命名分裂**：`log=` 74 处 vs `logger=` 59 处并存 | getLogger 归属统计 | 本轮 |
| 7 | **requirements 双轨+头部过时**：requirements.txt（70 行，头仍写「Python 3.10.11/runtime/py310」，实际主链 py312、lock 已按 py312 重导）与 lock（224 行）并存 | 文件头部直读 | 散乱轮 |
| 8 | **无 .gitattributes**——两次 CRLF 整文件翻转事故的根因级缺失 | 根目录清点 | 散乱轮 |
| 9 | **.ruff_cache 未被 gitignore 覆盖**（git check-ignore 实测 ✗；其余 5 个缓存已忽略） | 本轮 | |
| 10 | **巨石文件 8 个 ≥1950 行**：keyframe.py 2929 / video_engine 2754 / comic_asset 2505 / dialog_engine 2180 / dialog.py 2175 / comic_gen 2020 / video.py 1963 / models.py 1953（manga.py 4521→拆 8 模块有成功先例） | wc 实测 | 散乱轮 |
| 11 | **端点消费口径两悬案**：16 个仅测试/工具消费端点（draw/inpaint、draw/upscale、style resume-training 等 UI 无入口）+8 个死端点候选（全参数路径型，签名法盲区待人工终核） | 消费交叉 | 深查轮 |
| 12 | **14 处硬编码盘符路径**（E:\ 型，可移植性泄漏） | 全库正则 | 本轮 |
| 13 | **8 处裸 `datetime.now()`**——本地 naive 时间戳为主流口径（event_log 5 处为大户），与审计「时区 8h」项同根 | 逐处定位 | 本轮 |

### 🟢 卫生与口径级（7）

| # | 问题 | 证据 |
|---|---|---|
| 14 | qwen3-vl-2b 幽灵候选（API 返回+代码专判 dialog.py:658，manifest 无/磁盘无） | 总表轮 |
| 15 | ltx2 残留键（死功能活配置，被读） | 根查轮 |
| 16 | 3D 休眠 4 表（director_×3+scene_objects，仅 schema 层引用） | 根查轮 |
| 17 | 5 个可选依赖未钉版（lz4/reportlab/soundfile/basicsr/redis；lz4 运行时实证优雅降级） | 本轮 |
| 18 | 根目录旧构建 exe 物理残留（09-03，虽已 gitignore） | 散乱轮 |
| 19 | sakura.css 5 处 `!important`——集中同一性能/无障碍开关规则块（1110-1114），场景合理但未按铁律登记例外清单 | 本轮 |
| 20 | 路径命名轻度双轨：5 个 snake_case 段（ui_prefs/web_search/generate_h3_chain）vs 42 kebab 主流 | 散乱轮 |

## 二、核过为净清单（负面发现入账，防未来重复怀疑）

**影子路由 0**（同 method+path 重复注册）· **GET 写副作用 0** · **依赖方向倒置 0**（services/engines/data 无一反向 import api）· **模块级循环依赖 0** · **async 内 time.sleep 0**（36 处 sleep 全部归于 sync/线程函数，逐处归属实测）· **f-string SQL 注入 0**（5 处命中全为 PRAGMA/DDL 元命令，值来自内部常量）· **硬编码密钥字面量 0** · **前端 any=0 / console.log=0 / 非 Pascal 组件=0 / 孤儿组件=0** · **死表 0**（36 实体表全有代码引用）· **死模块 0**（挂载白名单全命中）· **manifest↔磁盘 17/17** · **except-pass 残留 19**（优于 09-17 治理口径 27）· **TODO/FIXME 0** · **logger 前缀统一 omnispace.\***。

## 三、总评与修复优先序

**体质判断**：结构骨架（挂载/建表/依赖方向/无环/无影子）异常坚实；问题集中在**纪律接缝**（标准双轨、外来子树未驯化、历史别名）与**资产口径**（幽灵候选/残留键/休眠表）——20 项中无一需要推倒，全部可渐进收敛。

建议序：**#8 .gitattributes（一行止血）→ #1 f5_tts 钉版（出包前必须）→ #2+#22 别名族收敛（独立复制函数先并回叠饰式）→ #21 信封归一（codemod 打法可复用）→ #4/#3 错误码与 Pydantic 归一 → #5 DF 子树驯化 → #23 data/ 整并（backup/backups 合一）→ #24 WS 主题专项对齐 → #10 巨石拆分排期 → 其余口径项随手清**。

### 第七轮·极致深度增补（8 项）

| # | 问题 | 证据 | 定级 |
|---|---|---|---|
| 21 | **68 处裸 dict 返回绕行响应信封**——经核**无自动包封中间件**（middleware 仅 RequestContext/EventLog/TrustedHost/LanAuth+error_handler），信封全靠手写；大户 comic_gen 10/hardware 9/comic_asset 9 | api 层 return 形态逐处计数（~410 信封 vs 68 裸） | 🟡 契约泄漏 |
| 22 | **别名实现三形态混用**（问题 #2 深化）——/paint 族一部分叠饰同函数（/paint/generate→draw_generate ✓），一部分独立复制函数（paint_task_cancel/paint_queue/paint_history_favorite 等 6+ 个），还有一函数服二径（art_inpaint 同服 /art/inpaint 与 /paint/inpaint）——独立复制件=双倍维护与漂移温床 | draw.py 装饰器-函数归属逐条比对 | 🟡 |
| 23 | **data/ 散居**——`backup`(4KB) 与 `backups`(400MB) 同名双目录并存（误用高危）；根级散落 6 个 JSON 备份件（art_styles_backup/removed×4/comic_assets_backup）；训练数据双约定（旧 lora_train/ 与新 lora_dataset/ 并存） | data/ 目录实清 | 🟡 |
| 24 | **WS 广播↔前端处理主题漂移：未能可靠测量**——本轮自动词汇对齐方法失效（交集为 0 的显假象）；已知 09-17 修过一批通知死信，当前漂移状况需专项人工对齐（后端 broadcast 话题表 vs 前端 switch 表） | 方法失效如实记录，不虚报 | 🟡 待专项 |
| 25 | 前端 **10 处硬编码 localhost:5800** 绕过集中式 `deriveApiBase()`（api.ts:32） | hardwareApi/ws.ts/HelpPage 等 4 文件 | 🟢 |
| 26 | 插件资产**三家园**（plugin/ 出厂件 + data/plugins/{imported,output} 用户件 + skills/ 第一方技能）——设计分层但缺一张口径登记图 | 目录实清 | 🟢 口径 |
| 27 | e2e 实弹测试 3 件错位于 tests/unit/（e2e_gpu_budget_live 等） | tests 布局 | 🟢 |
| 28 | async def 无 await 13/~108 个——抽样多为异常 handler//favicon 惯例形态，低气味 | 逐函数归属 | 🟢 |

**第七轮净区增补**：限流中间件已接线（setup_rate_limit, 300/min）✓ · TrustedHost+LAN 令牌闸 ✓ · models.py 字段 277 个 **Any 型=0**（铁律达标）· 可空型 78（28%，观察级）· 日志轮转正常（138MB 总量、单件 11MB×5 代）· flow_trace.db 17MB（30 天保留+VACUUM 今日已加 f610226）。

### 第八轮·三缝审计增补（用户点名：前端↔后端 / 后端↔ComfyUI / 前端↔ComfyUI 接缝）

| # | 问题 | 证据 | 定级 |
|---|---|---|---|
| 29 | **前端幽灵调用 1 条（双死残留）**：`systemApi.ts:108 getHealthInfo()` 调 `/health`（root 模式，后端无此路由）——且该函数**全前端零消费**=死出口调死路由；后端日志三次「路由不存在 /api/v1(/system)/health」佐证确有流量打空 | 145 前端调用 ↔ 320 路由全量对账，唯一幽灵 | 🟢 |
| 30 | **后端↔工作流 json 魔法节点 ID 暗耦合**：h3_chain_engine 以 `graph["110"/"1961"/"1700"…]` 硬编码改写工作流（9 个 ID、34 节点图中），当前全部在位✓，但**无加载时 ID 存在性校验、无节点 ID 冻结约定**——工作流 json 一经手改编号即隐性断链（仅 T8 插入路径有优雅降级，其余 8 处未证实有降级） | 引擎↔json 交叉核 | 🟢→🟡 脆弱性 |

**三缝净区（负面发现，质量很高）**：
①后端→ComfyUI **29 种节点类型全部在装、零缺失**（含 Z-Image 全家/PuLID/T8）✓；②**前端↔ComfyUI 直连=0**（无任何 8189/comfy 路径泄漏到前端，图片统一走后端媒体端点回传——架构分层干净）✓；③ComfyUI 端口**单源可控**（仅 h3_engine.py:56 一处 env 默认值定义，其余 8 处命中全为注释）✓；④前后端字段命名**全程统一 snake_case**（schema/types 抽查无 camelCase 漂移）✓；⑤前端 145 条调用 vs 后端 320 条路由，**有效幽灵仅 1 条且双死**——接缝健康度远超预期 ✓。

### 第九轮·分模块体检增补（用户点名 11 个功能模块逐房间检查）

**模块健康表**（端点数 / 专属测试 / 前端接线）：
AI对话 22/6/✓ · AI漫画 60/5/✓ · 写作台 28/2/✓ · 漫剧创作 39/5/✓ · 系统日志 13/**0**/✓ · 训练中心 42/7/✓ · 模型管理 28/7/✓ · 知识学习 42/6/✓ · 学习训练 12/**0**/✓(仅 LearnView 单文件域) · 帮助 0/0/✓ · 设置 38/**0**/✓ · 插件系统 11/**9**/✓

| # | 问题 | 证据 | 定级 |
|---|---|---|---|
| 31 | **三个模块零专属测试**：系统日志（13 端点）、设置（38 端点·全库最大模块）、学习训练（12 端点）——设置仅靠冒烟/信封级 4 件宽口径测试沾边；对照：插件系统 9 件专属测试是标杆 | tests/unit 按模块关键词全扫 | 🟡 |
| 32 | **对话别名分裂已渗入前端单文件**：dialogApi.ts 同一文件混用 /chat 族×4（send/stop/sessions/favorites）与 /dialog 族×4（status/prewarm/models/parse-document）——问题 #2/#22 的前端实锤，分裂不在后端 offerings 而在消费习惯 | dialogApi.ts 逐调用统计 | 🟡 |
| 33 | 写作台导出接口收裸 dict body（`novel/export body: dict`）——#3 的模块级典型例 | novel.py:611 | 🟢 |
| 34 | 帮助页**用户可见文案**硬编码「127.0.0.1:5800」——用户改端口后提示即误导（技术小白受害点） | HelpPage.tsx:43 | 🟢 |
| 35 | 训练中心注释漂移：docstring 写「两源任务」实导三源（knowledge/style/character） | training.py 头部 vs imports | 🟢 |
| 36 | **learn 与 learning 近孪生命名**：`/learn`（训练数据，前端 1 文件）vs `/learning`（学习会话，前端 12 文件）——认知负担实锤（本次审计的自动关键词也被绕晕） | 目录/路由对照 | 🟢 口径 |

**模块体检净区**：12 模块前端↔服务↔API 三件套**全接线无孤儿** ✓；训练中心三源聚合实现干净（统一形状+排序口径一致，仅 docstring 滞后）✓；插件系统测试密度全库第一（9 件：炸弹闸/沙箱/安全盾/技能插座全覆盖）✓；漫画域 60 端点但服务层/路由层分层清晰（comic_gen 0 路由=纯服务层）✓。

### 第十轮·屎山探测与文档对账增补（用户判断：「问题不止表面、是屎山、文档也有病」——逐条用数据裁决）

**屎山四大硬指标实测**：代码克隆（≥25 行函数体完全一致）= **0 组**；真死函数（无装饰器+全库含 tests 零引用）= **17 个**（非初测 234——那是叠装饰器路由的系统性误报，已修正）；bare except = **0**；上述均为「非屎山」证据。

| # | 问题 | 证据 | 定级 |
|---|---|---|---|
| 37 | **巨函数 22 个 ≥150 行**——TOP3：`keyframe._generate_keyframe_sync` **710 行**、`draw._run_generate_task` 543 行、`video_engine.generate` 533 行（paint_engine.load_model 431/vllm_service.start 427 随后）——单函数体量即正常人整文件，改动高风险区 | 全库函数体解析 | 🔴→🟡 屎山内核一号 |
| 38 | **前端巨组件 5 个 ≥900 行**：ModelManager.tsx **1547**、ComicWorkspace 1288、DialogView 1019、StoryboardTable 986、NovelPage 937 | wc 实测 | 🟡 |
| 39 | **深嵌套 3582 行 ≥6 层缩进**——可读性与回归风险地雷区 | 全库缩进扫描 | 🟡 |
| 40 | **except Exception 宽捕 1280 处**——「优雅降级」文化的暗面：错误被大量就地消化，真故障可能被淹没在降级噪音里（吞掉型 pass/continue 47 处已单列 #B2 系） | 全库统计 | 🟡 |
| 41 | **文档体量债（用户判断验证属实）**：docs/ 共 **119 份 md**，其中**方案/计划/记录类 58 份**（21 个不同日期戳=两个月日均近一份）；**28 份未入 INDEX**（含多份 round-* 审计报告长期游离）；INDEX 还引用了不在 docs/ 的 CLAUDE.md（路径写法微瑕） | INDEX↔磁盘双向对账 | 🟡 |
| 42 | 真死函数 17 个待清（asset_vault.encrypt_bytes/license_gate.deactivate/crypto_status/database.utcnow/logger.mask_value/rate_limit.get_limiter 等——清单级可删）+ CLAUDE.md 仍有 3 处旧口径（backend/ 路径、2.3.1、10 路由） | 修正版死码检测 | 🟢 |

**屎山裁决（大白话）**：典型屎山四件套里，「复制粘贴」（克隆 0）和「死码堆积」（仅 17）两件**不成立**——这个库的底盘纪律好于屎山标准；但「**巨函数/巨组件**」（#37/#38）与「**深嵌套+宽捕**」（#39/#40）两件**成立且是真正的山**，集中在漫剧/绘画/模型三个域的核心链路；文档债（#41）是第二座山。

---
*生成：监督会话（只读，十轮累计）。对账用法：本表为唯一问题入口；处置后在行内标注状态即可转为销账表。*

| # | 问题 | 证据 | 定级 |
|---|---|---|---|
| 43 | **三个「该接没接」断链病（B1 施工中揪出，attach_lora 同型）**：①`security_gate_enabled()` 全仓零调用——config `plugins.security_gate` 总闸**疑为摆设**（kernel_gateway.py:102 错误文案却承诺「关闭安全门禁后排查」，承诺与实现脱节）；②`module_model_allowed()` 模块白名单判定零调用（策略检查未执行或已内联他处待核）；③`write_refine_config()` 自述「供设置页调用」但设置页从未调（精修开关持久化断链） | 全仓引用终验 | 🟡 待核对接线 |
