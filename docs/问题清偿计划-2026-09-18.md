# 问题清偿计划（2026-09-18 深夜 · 基于《问题总账》42 项）

> **依据**：docs/问题总账-2026-09-18.md（十轮只读审计产物，42+ 项带证据）。
> **施工方**：原监督会话获用户令「整理成计划，然后开工」转型施工。
> **并发纪律**（§16.5/16.6）：另一会话在飞——①动每份文件前重读最新版 ②不重启后端 ③不跑 GPU 活 ④未经用户拍板不 commit ⑤60 分钟内彼会话碰过的文件（hardware/keyframe/models/spike_codec/file_store/rust_coding/knowledge_service/launcher）编辑时额外谨慎。

## 批次总表

| 批 | 内容 | 对应总账项 | 风险 | 状态 |
|---|---|---|---|---|
| **B0 止血卫生** | .gitattributes（CRLF 根修，**不立即 renormalize**避免一次性大 diff 撞车）+.gitignore 补 .ruff_cache + training.py「两源→三源」注释勘误 + learn/learning 域注释澄清 + HelpPage 端口文案去硬编码 | #8/#9/#35/#36/#34 | 极低 | 本轮施工 |
| **B1 死码清理** | 后端 17 个真死函数删除 + 前端 getHealthInfo 死出口（幽灵 /health 调用）删除 | #42/#29 | 低（删前逐名全仓复验引用，含 tools/scripts/launcher/updater/license_console/packaging_console） | 本轮施工 |
| B2 依赖钉版 | f5_tts 入 requirements-lock（需网络+安装验证）；lz4 等 5 个可选依赖定口径 | #1/#17 | 中（网络窗口） | 待网络恢复 |
| B3 别名收敛 | draw.py /paint 独立复制函数并回叠饰式；dialogApi.ts 统一单族 | #2/#22/#32 | 中（355 端点回归） | 待专属窗口 |
| B4 标准归一 | 错误码语义化（53 处裸数字）+ 信封归一（68 处裸 dict）+ Pydantic 化（93 处 dict body）——codemod 打法 | #4/#21/#3 | 中高 | 待拍板节奏 |
| B5 巨石拆分 | 710/543/533 行三大函数 + 前端 5 巨组件；深嵌套 3582 行随拆带出 | #37/#38/#39 | 高（核心链路） | 待拍板节奏 |
| B6 口径杂项 | vl-2b 幽灵定性、ltx2 键清、3D 休眠表处置、docs 28 份补 INDEX、CLAUDE.md 3 处旧口径 | #14/#15/#16/#41 | 低 | 排队 |

## B0+B1 四段制

1. **改动清单**：新增 `.gitattributes`；`.gitignore` +1 行；`src/api/training.py` docstring 一词；`src/api/learn.py` 头注一行；`frontend/src/components/help/HelpPage.tsx` 一句文案；`frontend/src/services/systemApi.ts` 删 getHealthInfo；后端约 15 文件各删 1-2 个死函数（asset_vault/license_gate/crypto/database/logger/rate_limit/graph_store/gpu_domains/readout/pkg/models/api 等）。
2. **影响面**：全部为「零引用实体删除/纯文本」，不改变任何运行行为；前端改动需下次构建生效（不在本轮构建，避免与彼会话撞 dist）。
3. **验证方法**：删前 17+1 个名字在全仓（src+tests+tools+scripts+launcher+updater+license_console+packaging_console+frontend）最终复验零引用；删后 `tools/ruff/ruff.exe check` 全绿 + 靶向 pytest（涉及文件的既有测试）通过。
4. **风险与回退**：最大风险=动态引用漏网（getattr 字符串形态）→ 复验已覆盖字符串出现即排除；回退=git checkout 单文件即可（全部未 commit 前）。

---
*执行记录见本文件下方追加。*

## 执行记录

### 2026-09-18 深夜 · B0+B1 已施工（未 commit，等拍板）
- **B0 完成**：.gitattributes 新增（未 renormalize，留安静窗口）；.gitignore +.ruff_cache；training.py「两套→三套」勘误；learn.py 域边界注释（/learn≠/learning）；HelpPage 端口文案去硬编码。
- **B1 完成（删 8 留 3）**：已删 is_legacy_manifest/crypto_status/reset_graph_store/get_sensitive_filter/get_limiter/run_cross_validation/_grid_image_prompt_from_abc/translate_batch_zh2en + 前端 systemApi health()/HealthInfo/桶出口三处（幽灵 /health 双死出口）。
- **删前全仓终验**：11 个候选（含 tools/scripts/launcher/updater/双冻结台/frontend 语料，剔除 vendored 大树）；**留 3 不删**（断链病嫌疑，见总账 #43）。
- **验证**：ruff 全绿；tsc --noEmit 0 错；py_compile 8/8；pytest test_api_smoke 14 passed（全 app 构建含删除后无 NameError）；残留引用 0。
- **新发现入账**：总账 #43（三个「该接没接」断链病：security_gate_enabled 总闸零调用=plugins.security_gate 开关疑为摆设（kernel_gateway:102 文案却承诺可关）；module_model_allowed 白名单判定零调用；write_refine_config 设置页持久化零调用）。

### 2026-09-18 深夜二刷 · B2+B6 已施工
- **B2 f5_tts 钉版完成**：f5-tts==1.1.22 + 14 伴生包按实测版本入 requirements-lock（gradio/torchcodec 记豁免注）；engine 真实调用面 infer_process/load_model/load_vocoder 导入链闭合实测；mypy.ini 补三方链 follow_imports=skip 豁免（voice_clone_engine 懒导入被跟踪进 site-packages 的连锁）。
- **B6 部分完成**：CLAUDE.md §2.3 路由数 10→11 勘误；ltx2 正名（config.yaml/config.py/video.py 六处 → audio_sync_max_duration_seconds 系）+ model_manager 三行死表项 ltx2_training 删除（零生产者实证）；INDEX 29 份补登记清零（含 v1 清单路由数 12→11 自勘误）。
- **B6 余项**：vl-2b 幽灵定性（待查 model_manager 种子清单）、3D 休眠 4 表处置（待拍板）。

---

# 剩余任务顺序队列（2026-09-19 起执行 · 用户令「一个个完成」）

> 排序原则：零风险调查/纯文本在前 → 需拍板项中置 → 三大工程批（B3/B4/B5）殿后按节奏。
> 每项完成即在 [ ] 打勾并记提交哈希；并发纪律延续（避彼会话在飞文件/改前重读/不擅闯）。

- [x] **Q1 vl-2b 幽灵候选定性 ✅（2026-09-19 00:35 完成）**：定性=**DB models 表历史种子僵尸**（非下载候选位）。实际清剿 14 行（vl-2b 兄弟×2/cogvideox×2/flux.1×2/kolors/sdxl×2/wan2.1×3/ltx-2，全部磁盘无+manifest 无+候选表无+无播种机制不会复活；wan2.1 系即 09-12 审计「wan21 空账」本体）。三重守卫后 sqlite backup API 备份 data/backups/omnispace-pre-vl2b-purge-20260919-003119.db 再删；models 表清零（真实登记源=manifest 17 条+磁盘扫描），活体 /models 复验零僵尸。总账 #14 销账
- [x] **Q2 小卫生一篮 ✅（2026-09-19 00:5x）**：requirements.txt 头部 py312 勘误（154 包历史数字一并除引）；根目录 09-03 旧 exe 删除（build_launcher_exe 可重建）；e2e 三件 git mv 归位 tests/ 根（pytest.ini 声明的设计之家，非新建子目录）；sakura !important 例外 E8 双登记（CSS 块内注释+例外清单.md）。总账 #7/#18/#27/#19 销账
- [x] **Q3 data/ 整并 ✅（2026-09-19 01:0x）**：backup/（内含远古迁移前 DB 快照×1，零代码引用）并入 backups/ 后移除；根级 5 个历史 JSON（art_styles 备份/removed×3/comic_assets 备份，零引用）归位 backups/；**art_styles_seed_final.json 留守**——勘误原判：它不是备份是打包金母版探针+金标测试双料真源（make_dist.py:397/test_style_pack_routing.py:46），归组错误已正。data/ 根仅余金种子+功能目录。总账 #23 销账
- [x] **Q4 三断链终审 ✅（2026-09-19 01:2x）**：①总闸平反（接线完好，B1 探测器误报认错）②③两真死函数已删（py_compile/ruff/冒烟+describe_refine 测试过/残留引用 0）。总账 #43 结案
- [x] **Q5 DF 子树驯化 ✅（2026-09-19 01:5x）**：print→log.info 收编 163 处（8 文件：train/trainer/distributedformer/multimodal_codec/real_dataset/spike_codec/readout/h3_chain_engine，均补 omnispace.* logger+__main__ 直跑补 basicConfig 防 INFO 被吞）；**两处故意豁免 22 处**：cutemamen/migrate.py×19（CLI 迁移工具 print 即界面，capsys 测试实证）+ sandbox_host×3（子进程 stdout=IPC 协议信道）。log/logger 变量名 59 文件+1 变体全归一为 log（139 统一/0 残留）。验证：ruff 全绿/distributed 106+冒烟 14 全过/print 残留=豁免数精确吻合。总账 #5/#6 销账
- [x] **Q6 工作流节点 ID 防呆 ✅（2026-09-19 02:2x）**：_render_graph 入口设 _REQUIRED_NODES 九节点闸（1/2/3/4/110/1700/1701/1706/1961），缺号 fail-closed 拒绝（H3_WORKFLOW_TEMPLATE_BROKEN+git 还原指引）——防模板手改编号漂移致静默错片。三测试：在库模板哨兵/缺号拒/完整过。总账 #30 销账
- [x] **Q7 可移植性小批 ✅（2026-09-19 02:4x）——含审计口径自我勘正**：①「14 处盘符泄漏」验尸后**真泄漏=0**（10 处=Windows 系统字体候选路径属合法系统资源引用、4 处=env 优先正确回退式，产品 Windows-only，改 env 派生属化妆故不做）；②datetime 实为 1 发射点+7 聚合点：发射点 event_log.ts 统一 aware 本地（+08:00 实测 `2026-09-19T01:24:21.156+08:00`，19 测试过），聚合点保持 naive 日界自洽（口径注明）。总账 #13 勘正销账
- [x] **Q8 三模块补测试 ✅（2026-09-19 03:0x）**：三件 26 测试（logs 10：信封/过滤/写读回路/空message拒/清理空态；system 7：设置回环/apikey GET打码/掩码回写保真/版本/信息/LAN令牌/ui_prefs；learn 6：空态/模型清单/状态形状/未知任务诚实失败/乱序载荷拒）。**开张即抓真回归**：stats 的 naive now 减 aware ts（Q7 时区改动的连带）当场暴露并修复（含历史 naive 数据归一兼容）。总账 #31 销账
- [x] **Q9 B3 别名收敛 ✅（2026-09-19 03:1x）——含 #22 勘正**：①draw.py「独立复制函数」证实为探测器误报（多行叠饰漏看，孪生探测实锤零复制漂移）——勘正入总账；②dialog 5 个 /dialog 独立端点补 /chat 正名 twin（叠饰零破坏+遗留别名冻结注）；③前端 6 处切 /chat（dialogApi×4/pluginApi×1/test×1，残留 grep=0）；④绘画前端本就统一 /draw 族无需动。验证：后端 29 测过/tsc 0 错/vitest 10 过/ruff 绿。总账 #2/#22/#32 销账
- [x] **Q10-1 错误码归一 ✅（2026-09-19 03:4x）**：54 处裸数字 ApiError → 语义串 53 处（经退休档案 78 条映射表自动转换 49+voice 5 手工=补 71003/71004 映射与文案），残留 1 处=cors.py:78 的 `code=1008`（WebSocket 关闭码，是 RFC 6455 协议常量非 ApiError 错误码，豁免不动）。总账 #4 销账。Q10-2 信封归一 ✅（2026-09-19 04:1x）：原判 68 处经 handler 级精扫后实为 **16 处真端点裸返回**（其余 52 处系内部 helper 非 API 面）；其中 10 处已精准包裹 ok()（learning idle 态/comic_asset batch+upload/keyframe batch+story/models vllm_stop+export/system diagnose），6 处为多行复杂 dict 保守延后。总账 #21 部分销账（10/16）。Q10-3 Pydantic 化（#3）排后
- [ ] **Q11 B5 巨石拆分**（#37/#38/#39，需拍板节奏）
- [ ] **Q12 专项：WS 主题人工对齐**（#24）+ 挂起观察区（#40 宽捕收窄/#11 端点口径/#17 可选依赖）
