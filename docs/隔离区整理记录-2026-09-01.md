# 隔离区整理记录 — 2026-09-01

执行 `docs/体积优化计划-2026-08-31.md` **第 1 级（绝对零影响）**，共约 60G 同盘移入隔离区 `E:\_trash_staging\2026-09-01\`（明细与恢复方法见该目录下 MANIFEST.md）。**未删除任何文件**，观察期确认无异常后清空隔离区才实际释放磁盘（预计 E 盘可用 104G → ~164G）。

## 执行内容
- **A1** sd15 顶层散件 ×4 + 全部 `*.bin`（28G）：`_find_sd15_base()` 只认 model_index.json 布局，diffusers 优先 safetensors。
- **A2** sdxl fp32 影子 ×4（13G）：fp16 伴随文件存在即 `variant="fp16"`。
- **A3** vace UMT5 三分片改挂 wan22 同源硬链接（11.4G 冗余消除，双路径照常可用；字节级三验全对）。
- **A4/A5** bge-m3 `.cache` 下载半成品 + `pytorch_model.bin`（5.8G）。
- **A6** ComfyUI `output/h3_chains` 测试视频（1.8G）。
- **A7** runtime 安装包残留 ×5（619M，全库零引用）。
- **A8** `.trae-html-share-packages`、`.mimosa`（249M）。

## 当日新发现（与 08-31 计划出入）
1. **bge-m3 全目录 8.0G 代码零引用**：vector_db.py 硬编码 `embed/bge-large-zh`，src/skills/scripts 全库无 bge-m3 加载点——计划中"OmniLearn 在用"结论存疑，整目录可否一并隔离**待拍板**。
2. h3_chains 实际 1.8G（计划记 5.5G，部分已被系统自动清理）。
3. `runtime/node20/`（67M）疑似 node 重复解压，零计划内引用，未动待查证。

## 验证记录
- 代码引用复核：sd15/sdxl 加载路径、ComfyUI user 模板绝对路径、make_dist/portability_check、models_manifest（目录级登记，无悬空）——全部零命中。
- A3 字节级校验：三分片大小 + 头/尾 64MB MD5 全对；挂接后链接数=2。
- 后端冒烟（23:52 实测）：boot.py 正规链点火 → **20s 就绪**，/health healthy、db ok；模型注册表 `/api/v1/models/list` 全量在列（sd15/sdxl-base-1.0/bge-m3/bge-large-zh/AnimateLCM/wan21-vace/wan22-ti2v 均命中）；OmniLearn `/api/v1/knowledge/stats` 1761 条、chroma+bge-large-zh 后端正常；`/api/v1/learn/knowledge/search` 语义检索返回带分值命中（0.699）。冒烟后 `launcher/stop.py` 正规优雅退出，零残留进程。
- `scripts/dir_audit.py` 复跑：**结论"干净，符合规范"，退出码 0**（含 ComfyUI↔models 7 对硬链接合规项）。

## 未动（第 2/3 级，待拍板）
第 2 级 49.2G（awq TE / int8-fl2va / Ref2VA nvfp4——ComfyUI 手动模板在用）、第 3 级 49.8G（klein-4b / 9b diffusers 分片——需改代码）。
