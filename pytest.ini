[pytest]
testpaths = backend/tests
pythonpath = .
addopts = -ra --strict-markers
markers =
    smoke: 冒烟测试（关键路径可用性，必须在任何提交前通过）
    schema: 数据库模式与迁移测试（拦截列缺失类事故）
filterwarnings =
    ignore::DeprecationWarning
