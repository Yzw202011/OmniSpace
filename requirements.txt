# OmniSpace AI 后端依赖（开发主链 Python 3.12.10 / runtime/py312，B1 批 2026-09-13 起；
# py310 仍并存=回退锚点与打包基线，出包对齐口径见 tools/make_dist.py）
#
# 依赖声明权威关系（2026-08-19 依赖归一化裁定 TASK-P0-02；2026-09-18 勘误头部口径）：
#   - requirements-lock.txt = 唯一权威：精确钉版，实际运行环境导出（包数以该文件实数为准，勿引用历史数字）
#   - 本文件 = 开发意图参考：宽松下限约束，不保证与实际环境一致
#   - 重建环境：pip install -r requirements-lock.txt（勿用本文件）
#   - src/requirements.txt 已删除（v2.1 陈旧版，声明了未使用的 sqlalchemy）
# --- Web 框架 ---
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.10
pydantic-settings>=2.2
python-multipart>=0.0.9
aiofiles>=23.2
aiohttp>=3.9
httpx>=0.27
sse-starlette>=2.1
websockets>=12.0
pyyaml>=6.0

# --- AI 推理 ---
# numpy 上限放开（2026-09-01 P1 裁决）：requirements-lock 锁 2.2.6 且 pydeps/site-packages
# 实际双树共存均按 2.x 运行，原 <2.0 与现实冲突
numpy>=2.2,<3.0
torch>=2.8
transformers>=4.46
diffusers>=0.31
accelerate>=0.29
safetensors>=0.4
einops>=0.7
omegaconf>=2.3
timm>=0.9
sentencepiece>=0.2
Pillow>=10.0
opencv-python>=4.9
imageio>=2.34
imageio-ffmpeg>=0.4

# --- 训练（LoRA） ---
peft>=0.10
datasets>=2.19

# --- 数据与向量 ---
chromadb>=0.5
sentence-transformers>=2.7
modelscope>=1.16

# --- 系统监控 ---
pynvml>=12.0
psutil>=6.1
pystray>=0.19
pywin32>=306
cryptography>=41.0

# --- 视觉模型 ---
ultralytics>=8.2
segment-anything>=1.0

# --- TTS ---
# 注：AI TTS 权重已隔离待语音路线拍板（拍板清单 D1b），当前合成走
# comtypes 直连 SAPI5；librosa 等保留待路线定案
librosa>=0.10
soundfile>=0.12
cn2an>=0.5
pypinyin>=0.50
jieba_fast>=0.53

# --- 测试 ---
pytest>=8.0
pytest-asyncio>=0.23
