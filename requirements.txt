# OmniSpace AI 后端依赖（Python 3.10.11 / Windows x64，嵌入式 runtime/py310）
#
# 依赖声明权威关系（2026-08-19 依赖归一化裁定，TASK-P0-02）：
#   - requirements-lock.txt = 唯一权威：154 包精确钉版，实际运行环境导出
#   - 本文件 = 开发意图参考：宽松下限约束，不保证与实际环境一致
#   - 重建环境：pip install -r requirements-lock.txt（勿用本文件）
#   - backend/requirements.txt 已删除（v2.1 陈旧版，声明了未使用的 sqlalchemy）
# --- Web 框架 ---
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.10
pydantic-settings>=2.2
python-multipart>=0.0.9
aiofiles>=23.2
aiohttp>=3.9
httpx>=0.27
sse-starlette>=2.1
websockets>=12.0
pyyaml>=6.0
loguru>=0.7

# --- AI 推理 ---
numpy>=1.26.4,<2.0
torch>=2.8
transformers>=4.46
diffusers>=0.31
accelerate>=0.29
safetensors>=0.4
einops>=0.7
omegaconf>=2.3
timm>=0.9
sentencepiece>=0.2
Pillow>=10.0
opencv-python>=4.9
imageio>=2.34
imageio-ffmpeg>=0.4

# --- 训练（LoRA） ---
peft>=0.10
datasets>=2.19

# --- 数据与向量 ---
chromadb>=0.5
sentence-transformers>=2.7
modelscope>=1.16

# --- 系统监控 ---
pynvml>=12.0
psutil>=6.1
pystray>=0.19
pywin32>=306
cryptography>=41.0

# --- 视觉模型 ---
ultralytics>=8.2
segment-anything>=1.0

# --- TTS ---
librosa>=0.10
soundfile>=0.12
cn2an>=0.5
pypinyin>=0.50
jieba_fast>=0.53
pyttsx3>=2.90

# --- 安全沙箱 ---
RestrictedPython>=7.0

# --- 测试 ---
pytest>=8.0
pytest-asyncio>=0.23
