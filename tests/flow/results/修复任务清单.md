# OmniSpace AI v2.3.1 非 PASS 用例修复任务清单

> 来源：《全功能模块操作流程测试计划（极致颗粒细度增强版）》474 条用例实测结果
> （tests/flow/results/*.json，2026-08-09）。
> 非 PASS 共 **299 条**：FAIL 57 / DEGRADED 131 / SKIP 111。
> 本文档按"可修复性 + 修复批次"组织全部 299 条，每条给出问题定位与修复方案。

## 0. 分诊总览

| 分诊类别 | 数量 | 可修复性 | 处置批次 |
|---|---|---|---|
| D. 端点/字段缺失（代码可修复） | 76+10=86 | ✅ 本轮修复 | 批 1~6 |
| B. 纯前端交互（浏览器冒烟可转 PASS） | ~62 | ✅ 本轮验证 | 批 7 |
| A. 模型资产未随包（FLUX/Kolors/LTX-2/Wan2.1/CogVideoX/IP-Adapter/ControlNet-OpenPose/Real-ESRGAN/8B+ 对话模型等） | 22 | ❌ 需下载数百 GB 权重，保持诚实降级 | 不修 |
| E. 物理环境依赖（低配机/拔网线/物理换卡/8 小时长稳） | 5 | ❌ 自动化环境不可测 | 不修 |
| Z-前端职责确认（Three.js 相机/吸附/多选等 3D 交互） | ~40 | 🔶 部分可冒烟验证，部分为前端渲染固有职责 | 批 7 |
| Z-学习模块内部逻辑（ReAct/DOM解析等无独立端点） | ~14 | 🔶 经日志/报告端点间接验证 | 批 7 |

**修复目标**：FAIL 57 → 0（或转为有依据的 DEGRADED）；DEGRADED 131 → 显著下降；SKIP 111 → 可浏览器验证的全部转 PASS。

---

## 批 1：漫剧创作模块（OmniComic）— 47 FAIL + 关联 DEGRADED/SKIP

### 1.1 项目 CRUD（COMIC-001/002/003/004，P0/P1/P2/P1）

| 用例 | 问题 | 修复方案 |
|---|---|---|
| COMIC-001 | 无 `POST /comic/project/create` | 新增项目表 CRUD：projects 表已有（分镜自动建立），补独立端点：create(name, template?)→project_id；名称非空校验 |
| COMIC-002 | 无模板预置分镜结构 | create 支持 template=comic_drama：自动写入 5 行预置分镜（开场→发展→冲突→高潮→结尾） |
| COMIC-003 | 无重名校验 | create 时同名 → 409 语义错误码 `COMIC_PROJECT_NAME_DUPLICATED` |
| COMIC-004 | 无编辑/删除/切换 | `PUT /comic/project/{id}`（改名）、`DELETE /comic/project/{id}`（级联删分镜行）、`GET /comic/project/list` |

### 1.2 DSL 文件上传（COMIC-005/009/010，P0/P1/P2）

| 用例 | 问题 | 修复方案 |
|---|---|---|
| COMIC-005 | 无 multipart 上传通道 | `POST /comic/script/import-dsl`（UploadFile，.txt/.dsl，≤10MB），复用现有 JSON 文本导入管线 |
| COMIC-009 | 无 `shot:` 标记仍导入成功 | DSL 校验模式参数 strict=true 时无 shot 标记 → `COMIC_DSL_FORMAT_INVALID` |
| COMIC-010 | 无格式/大小校验 | 扩展名白名单 + 10MB 上限 + 空文件拒绝，统一语义错误码 |

### 1.3 分镜字段扩展（COMIC-058/059/061/062/069，P1）

storyboard_rows 表 ALTER TABLE 新增列（启动迁移，IF NOT EXISTS）：

| 字段 | 类型 | 校验 | 用例 |
|---|---|---|---|
| camera_type | TEXT | 特写/近景/中景/全景/远景/俯拍/仰拍/主观镜头 8 枚举 | COMIC-058 |
| camera_angle | TEXT | 平视/俯视/仰视/侧视/背面 5 枚举 | COMIC-059 |
| camera_movement | TEXT | 推/拉/摇/移/跟/甩/升/降/静止 9 枚举 | COMIC-059 |
| duration | REAL | 1~60s | COMIC-061 |
| transition | TEXT | 淡入淡出/叠化/闪白/闪黑/推拉/无 6 枚举 | COMIC-062 |
| speed/volume | REAL | 0.5~2.0 / -12~0dB | COMIC-069 |
| sort_index | INTEGER | 拖拽排序持久化 | （记忆 P1 项） |

`PUT /manga/storyboard/row` 接受新字段并枚举校验；非法值 → `SYS_PARAM_INVALID`。

### 1.4 资产图链路（COMIC-025~037、138，P0~P1）

| 端点 | 方案 |
|---|---|
| `POST /comic/asset/generate-character` | 走 SDXL paint_engine 文生图（角色提示词模板），产物落 `data/comic_assets/{project_id}/characters/{name}/portrait.png`，登记资产表 |
| `POST /comic/asset/generate-scene` / `generate-prop` | 同管线，场景/道具模板；道具加透明背景（PIL alpha 通道处理） |
| `POST /comic/asset/batch-generate` | 队列化批量（逐条提交 paint 任务，聚合 task_id 列表） |
| `GET /comic/asset/library` | 资产清单（类型/项目过滤、缩略图 URL） |
| `PUT /comic/asset/bind` | 资产↔分镜行绑定（storyboard_rows.asset_id） |
| `POST /comic/asset/export-pack` | 项目资产 zip 打包（含 manifest.json） |
| 多视图 turnaround（033/034/035/040~044） | FLUX 未随包；SDXL 单视图 + PIL 四宫格画布裁切实现 4096×1024 布局，一致性校验以色调直方图方差实现；深度一致性如实 DEGRADED |
| 一键去背（036） | PIL 通道分离+阈值抠图（无 SAM 时的经典算法）；SAM 已随包可后续接 /art/segment |

### 1.5 音色上传/克隆（COMIC-047/048/053/054）

| 端点 | 方案 |
|---|---|
| `POST /manga/voices/upload` | multipart 音频（wav/mp3/flac/m4a，≤20MB，ffprobe 校验时长 3~60s），落盘 voices 目录 |
| `POST /manga/voices/clone` | GPT-SoVITS 权重随包但缺官方推理代码包+pypinyin → 端点存在但如实返回 `VOICE_CLONE_UNAVAILABLE`（DEGRADED 转有依据）；SAPI5 试听链路不变 |

### 1.6 关键帧 CRUD（COMIC-121~125，P0/P1）

| 端点 | 方案 |
|---|---|
| `POST /manga/keyframe/generate` | 分镜行画面描述→SDXL 文生图→落 `data/keyframes/{row_id}/v1.png`，keyframe 表登记（row_id/version/path/prompt） |
| `POST /manga/keyframe/batch` | 批量（行 id 列表），队列化 |
| `POST /manga/keyframe/regenerate` | 新版本 v{n+1}，保留旧版可回退；`POST /manga/keyframe/rollback` |
| `DELETE /manga/keyframe/{id}` | 删记录+文件 |
| 失败路径（125） | SDXL 未就绪 → `PAINT_ENGINE_NOT_READY` 如实报错 |

### 1.7 其余端点

| 用例 | 端点 | 方案 |
|---|---|---|
| COMIC-070 | `POST /manga/storyboard/emotion-detect` | 台词语义→情绪标签：规则词典（喜怒哀乐惊等关键词）+对话引擎可用时走 LLM 分类；引擎未就绪→DIALOG_NOT_READY |
| COMIC-090 | `PUT /comic/scene/object/update` | 3D 对象 Transform 持久化（scene_objects 表：project_id/object_id/position/rotation/scale，upsert；防抖为前端职责，后端全量接受） |
| COMIC-105 | `POST /director/text-to-3d` | **TripoSR 已随包 1.6GB 权重接线**：subprocess/管线推理 image→mesh（glb 输出）；权重缺→`MODEL_NOT_READY` |
| COMIC-131 | `POST /manga/video/{task_id}/cancel` | 视频任务取消旗标（encoder 循环检查点退出，状态→cancelled） |
| COMIC-135/136 | `GET /manga/storyboard/{pid}/export?format=png-seq/pdf` | png-seq：分镜关键帧/占位图 zip；pdf：reportlab 生成分镜脚本 PDF（无 reportlab 则用 PIL 图文合成 PNG 后转 PDF） |
| COMIC-139 | `POST /comic/export/bundle` | 多内容打包（分镜 csv/json + 资产 zip + 视频）合并 zip |

---

## 批 2：视频风格模块（VideoStyle）— 10 FAIL

| 用例 | 端点/能力 | 修复方案 |
|---|---|---|
| STYLE-017 | `POST /style/tasks/{id}/pause|resume|cancel` | 训练工作线程加控制旗标（epoch 边界检查点）；pause→挂起旗标、resume→清除、cancel→中断置 cancelled |
| STYLE-018 | 断点续训 | train() 支持 resume_from 参数（加载 adapter 权重继续）；`POST /style/tasks/{id}/resume-training` 从最近 checkpoint |
| STYLE-023 | preview 强度参数 | `/style/preview` 接受 strength 0~1（LoRA scale 注入推理；LTX-2 未随包时参数校验仍生效） |
| STYLE-024 | `POST /style/{style_id}/merge` | 多 LoRA 权重按比例线性融合（safetensors 张量加权和），产出新预览 |
| STYLE-025 | `POST /style/export` | adapter_model.safetensors + meta.json 打 tar.gz，附 SHA256 校验文件 |
| STYLE-026 | 风格应用到视频 | `/video/generate` 请求模型加 style_lora/style_strength 参数（LTX-2 缺时参数接受+degraded 标记） |
| STYLE-027/028 | `GET /style/list`、`PUT/DELETE /style/{id}` | 风格项目=版本实体：列表（名称搜索/状态筛选）、重命名（meta.name）、删除（训练中拒绝 `STYLE_TRAINING_LOCKED`） |
| STYLE-030 | `GET /style/{id}/metrics` | meta.json quality_score + 训练记录聚合；CSV/JSON 导出 |
| STYLE-032 | `POST /style/clone`、`/style/templates` | 数据集+配置复制为新任务；模板存 settings 表 |

---

## 批 3：知识学习模块（OmniLearn）— 28 DEGRADED

| 用例 | 修复方案 |
|---|---|
| LEARN-003 | 主题名唯一约束：create 时同名 → `LEARN_TOPIC_NAME_DUPLICATED`（红字提示前端做） |
| LEARN-004/067 | `topic/create` 加 depth 参数（shallow/standard/deep → max_pages 8/20/50 映射） |
| LEARN-005 | topic 模型加 seed_urls 字段（JSON 数组），会话优先从种子 URL 开始 |
| LEARN-006 | `topic/list` 加 keyword/status/sort 查询参数（SQL 过滤排序） |
| LEARN-032 | `/learn/knowledge/graph/export?format=gexf|graphml`（networkx 写盘） |
| LEARN-035 | `GET /learn/lora/versions/compare?a=v1&b=v2`（质量分/样本数/超参对比） |
| LEARN-037 | 资源阈值设置扩展 cpu_percent/mem_percent/bandwidth_mbps 字段读写 |
| LEARN-040 | `/learn/knowledge/list` 加 type/min_score 筛选参数 |
| LEARN-041 | `PUT /learn/knowledge/{id}` 编辑（content/topic/type） |
| LEARN-046 | `POST /learn/knowledge/import`（json/csv 文件，merge_strategy=append/overwrite/dedup） |
| LEARN-049/050/051/052 | 分析端点：efficiency（提取率/每小时）、sources（域名聚合/可信度）、trend（30 天按日聚合）、topic-compare（多主题对比）——全部 SQL 聚合实现 |
| LEARN-053/054 | 定时触发接线：scheduler 后台循环检查 schedule_windows + 对话不确定性旗标消费（TRIGGER_DIALOG_GAP 落队） |
| LEARN-056 | 主题学习排队：61008 冲突时入 waiting 队列，当前会话结束自动启动下一个 |
| LEARN-063 | 容量上限接近告警字段（≥90% 时 status 返回 near_capacity=true） |
| LEARN-066 | 知识条目加 lang 字段（提取时 langdetect/规则判定） |
| LEARN-069 | `POST /learn/topic/clone`（复制主题设置，可选含知识） |
| LEARN-012/013/015/016/017/018/019/028/029/033/045/057 | 内部逻辑/已有等价验证 → 批 7 经日志/报告端点间接复核后转 PASS 或保留说明 |

---

## 批 4：AI 绘画模块（OmniDraw）— 10 DEGRADED + 1 SKIP

| 用例 | 修复方案 |
|---|---|
| PAINT-027/028/030/031、COMIC-039 | `POST /art/inpaint`：SDXL InpaintPipeline（diffusers 布局随包）；遮罩为空/过大校验；模型缺→如实 DEGRADED |
| PAINT-041/042/044 | 绘画任务控制：`POST /paint/task/{id}/cancel`；批量队列优先级字段+顺序调整端点 |
| PAINT-046 | 画廊筛选：`/paint/history` 加 favorite/时间范围/尺寸筛选参数 |
| PAINT-048 | paint_history 加 favorite 列 + `POST /paint/history/{id}/favorite` |
| PAINT-050/051 | `DELETE /paint/history/{id}` + 批量删除（ids 数组） |
| PAINT-019/020/033/037/039 | ControlNet canny/depth 已部分接线（MiDaS 响应正常）、LANCZOS 超分为模型缺失降级 → 保留 DEGRADED 依据 |

---

## 批 5：设置/系统模块（Settings）— 15 DEGRADED

| 用例 | 修复方案 |
|---|---|
| SET-008 | `PUT /hardware/tier` 手动档位设置（覆盖自动检测，写 settings 持久化） |
| SET-010 | 推理线程数/缓存设置端点（写 settings，dispatcher 读取） |
| SET-012/013/014 | 代理/镜像源/带宽设置持久化端点（settings 表读写；离线定位如实说明仅学习模块联网生效） |
| SET-015 | 训练默认参数端点：`GET/PUT /learn/train/defaults`（rank/alpha/lr/epochs，新建训练读取） |
| SET-019 | 自动备份开关+间隔设置（backup scheduler 已有手动链路，加定时触发） |
| SET-021 | 全量数据导出 `.tar.gz + SHA256`（复用项目级导出扩展） |
| SET-023 | API Key 管理端点（bcrypt 哈希存储、脱敏显示、删除） |
| SET-025/026 | 日志级别运行时设置（logging.setLevel）+ `GET /system/logs`（分页/导出.txt/保留天数清理） |
| SET-029 | `POST /system/restart`（确认 token，延迟自重启线程；浏览器形态如实说明无桌面壳） |
| SET-030 | `GET /system/disk`（各卷概览/数据目录占用/top 大文件） |

---

## 批 6：模型管理 + 已随包模型接线 — 8 DEGRADED + 3 条

| 用例 | 修复方案 |
|---|---|
| MODEL-019/021 | `/models/download`：离线定位 → 保留 DEGRADED 依据（或实现 HF 镜像下载，需联网） |
| MODEL-023 | `/models/update` 版本检查：本地 manifest 版本 vs 远程（离线时如实返回 offline） |
| MODEL-033 | 显存碎片率：torch.cuda.memory_stats 计算 fragmentation=1-largest_free_block/free |
| MODEL-034 | `PUT /models/config` 量化精度偏好持久化（下次加载生效，诚实标注不在途切换） |
| MODEL-036 | 模型详情加 dependencies 字段（manifest 声明依赖 VAE/CLIP 等） |
| MODEL-037 | `/models/export`：模型目录 tar.gz 打包+SHA256 |
| MODEL-038 | `/models/benchmark`：对已加载模型跑 N 次推理测 Tokens/s/显存峰值，历史落库对比 |
| COMIC-105 | TripoSR 接线（见批 1.7） |
| PAINT-020 | MiDaS 深度端点已响应 → 复核转 PASS |

---

## 批 7：SKIP/前端交互 → 浏览器冒烟转 PASS（~100 条）

用 Playwright（headless Chromium）逐条验证并更新用例状态：

- SYS-008/009/012/018/020~026：导航切换/折叠/快速切换/面板交互
- CHAT-012/014/017/018/022/038/046：上传压缩/中断重连/Markdown 渲染/复制/引用/输入建议
- PAINT-003/028/037/039 等：快捷标签/画笔/风格下拉
- COMIC-015/023/038/045/052/064/066/069/072~092/100~107：分镜模式切换/搜索/3D 导演台交互（相机/吸附/多选/分组/工具栏/降级）
- SET-001~007 等：设置页交互
- CROSS-部分：UI 冒烟残留项

### 批 7 执行结果（2026-08-09，UI 冒烟四批次全绿）

| 测试脚本 | 覆盖 | 结果 |
|---|---|---|
| ui_batch_chat.py | 对话页 12 条 | **12 PASS / 0 FAIL / 0 SKIP** |
| ui_batch_comic_a.py | 分镜表+音色绑定 18 条 | **13 PASS / 0 FAIL / 5 SKIP** |
| ui_batch_comic_b.py | 3D 导演台 17 条 | **7 PASS / 0 FAIL / 10 SKIP** |
| ui_batch_sys.py | SYS/SET/STYLE/MODEL 21 条 | **13 PASS / 0 FAIL / 8 SKIP** |
| **合计** | **68 条** | **45 PASS / 0 FAIL / 23 SKIP** |

批 7 修复项（本轮开发）：
- COMIC-020 镜号点击 → 行级选中联动右侧「选中分镜」属性面板（useMangaStore.selectedRowId）
- COMIC-021 行右键菜单被 window 级 contextmenu 监听瞬间关闭 → stopPropagation 修复
- COMIC-014 AI 自动分镜两段式（生成预览 → 确认导入），防误写
- COMIC-066 描述词模板下拉（5 套写作词组，未选中行时如实 warning）
- COMIC-007 DSL 语法示例 + 复制按钮（剪贴板真实写入）
- COMIC-069 语速 0.5~2.0 / 音量 -12~0dB 行级滑块（松手/键起提交 PUT，越界 40008）
- COMIC-079 3D 射线选择失效（camera.parent=null）→ SelectorConfig 显式传 scene 修复
- SYS-020 右侧面板左缘拖拽调宽 + localStorage 持久化
- CHAT-012/038 超大图片前端 10MB 上限校验与 toast
- STYLE-009/010/011 预设模板/显存预估/恢复默认（前端早已实现，本轮补齐脚本真实交互验证）
- MODEL-002/004/013 模型排序/搜索/拖拽导入（同上，脚本补验）

批 7 残留 SKIP（23 条，均为前端无入口的诚实 SKIP，转后续开发评估）：
- COMIC-015 自动/手动分镜模式开关、COMIC-038 资产四视图预览弹窗、COMIC-056 时长字段、COMIC-052 角色级语音合成入口、COMIC-064 AI 描述生成按钮
- COMIC-075 双击聚焦/F 重置、COMIC-076/077/078 导演台资产库面板与拖拽、COMIC-082 外部资产加载错误路径、COMIC-087 Transform 数值属性面板、COMIC-088/089 网格/表面吸附、COMIC-091 多选、COMIC-092 编组
- SET-002 语言、SET-004 开机自启、SET-005 通知、SET-006 快捷键、SET-009 渲染质量、SET-018 导出质量预设、SET-022 隐私
- MODEL-028 模型预热入口

## 不修复（27 条，保留依据）

- A 类 22 条：FLUX.1-dev、Kolors 2.1、ControlNet-OpenPose、IP-Adapter-Plus、LTX-2、Wan2.1-14B/1.3B、CogVideoX-2B/5B、Qwen3-VL-8B/2B、Qwen3-14B/ChatGLM-4-9B/Yi-1.5-9B、Stable Zero123/Wonder3D/LGM、Real-ESRGAN、CLIP/Blip2、vLLM、CosyVoice3/ChatTTS、Tauri 桌面壳、SQLCipher
- E 类 5 条：SYS-005（低配机）、CROSS-017（物理换卡）、SYS-028/029/031 窗口管理（浏览器形态）

---

## 执行顺序与状态追踪

| 批次 | 范围 | 影响用例数 | 状态 |
|---|---|---|---|
| 批 1 | 漫剧模块端点 | ~55 | ✅ 完成（冒烟全过） |
| 批 2 | 视频风格端点 | 10 | ✅ 完成（17/17） |
| 批 3 | 学习模块增强 | ~20 | ✅ 完成（33/33） |
| 批 4 | 绘画画廊/inpaint | ~8 | ✅ 完成（37/37，含取消传导修复） |
| 批 5 | 设置/系统端点 | ~15 | ✅ 完成（12/12） |
| 批 6 | 模型管理/接线 | ~8 | ✅ 完成（13 PASS + 1 诚实 DEGRADED：COMIC-105 TripoSR 真实出网格 79K 顶点/1.8s；MODEL-038 基准 9.99 tok/s 落库；MODEL-037 导出 tar.gz+SHA256；MODEL-019 下载离线门控保留依据） |
| 批 7 | 浏览器冒烟 | 68 | ✅ 完成（45 PASS / 0 FAIL / 23 诚实 SKIP 转后续开发评估；前端 dist 已重建，chat/comic_a/comic_b/sys 四批次回归全绿） |
| 回归 | 全量重跑+报告 | 474 | ⏳ 待开始 |
